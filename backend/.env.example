# Shared by API (P1) and MCP server (P2)
QUAESTOR_DB=sqlite:///quaestor.db
APP_TOKEN=change-me

# API only
APP_PASSWORD=change-me
SESSION_SECRET=change-me-32-chars-minimum
COOKIE_SECURE=false
FRONTEND_ORIGIN=http://localhost:3000

# MCP server only (defaults shown)
MCP_HOST=0.0.0.0
MCP_PORT=9000
