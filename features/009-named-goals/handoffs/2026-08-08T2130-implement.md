---
skill: atdd-team
agent_id: main-session
feature: 009-named-goals
started: 2026-08-08T2000
ended: 2026-08-08T2130
checkpoint: 5
artifacts:
  - backend/src/quaestor/domain/models.py
  - backend/src/quaestor/domain/dtos.py
  - backend/src/quaestor/domain/rules.py
  - backend/src/quaestor/services/metas.py
  - backend/src/quaestor/services/funds.py
  - backend/src/quaestor/services/month_aggregate.py
  - backend/src/quaestor/services/transactions.py
  - backend/src/quaestor/api/schemas.py
  - backend/src/quaestor/api/routers/metas.py
  - backend/src/quaestor/api/routers/transactions.py
  - backend/src/quaestor/migrations/versions/0013_a_meta_is_a_named_thing_to_save_for.py
  - backend/src/quaestor/migrations/versions/0014_a_cancelled_meta_says_which_month_it_freed.py
  - backend/src/quaestor/migrations/versions/0015_the_fund_no_longer_saves_toward_a_date.py
  - backend/src/quaestor/migrations/versions/0016_a_meta_edit_starts_in_the_month_it_was_made.py
  - backend/tests/api/test_metas.py
  - frontend/app/(app)/metas/
  - frontend/components/meta-field.tsx
  - frontend/lib/api/metas.ts
  - frontend/lib/metas.ts
exit_criteria:
  - criterion: Every acceptance scenario is bound and green
    verified_by: tool
    met: true
    evidence: "`./run-acceptance-tests.sh`: 009 spec-coverage `scenarios 125 / unbound 0`; 010 `unbound 0`; pytest `472 passed`; vitest `55 files, 397 passed`."
  - criterion: The unit suite is green
    verified_by: tool
    met: true
    evidence: "`cd backend && SESSION_SECRET=… uv run pytest -q` → `1010 passed`."
  - criterion: The lint gate is green
    verified_by: tool
    met: true
    evidence: "`just lint` → ruff check, ruff format --check, biome check and tsc --noEmit all clean. Biome's 4 warnings and 1 info are pre-existing and in files this feature did not touch."
  - criterion: The bounded query count holds
    verified_by: tool
    met: true
    evidence: "`test_load_issues_bounded_query_count` passes at 14. It caught three separate raises during implementation; the bound was corrected in ADR-0046 and plan.md each time rather than loosened in the test."
  - criterion: Migrations rehearsed before any touch real data
    verified_by: tool
    met: true
    evidence: "0013 applied to the local Postgres by the owner behind a fresh backup, verified read-only afterwards: 635 movements, 43 categories, 0 funds, all unchanged. 0014, 0015 and 0016 rehearsed up and down on throwaway SQLite; none applied. 0015 refuses when any fund still uses the dated rule, demonstrated live — the SQLite sandbox has one such fund and 0015 aborted its container's startup with the intended message."
  - criterion: Verification independence
    verified_by: judgment
    met: false
    evidence: "NOT MET AND NOT CLAIMED. One agent wrote the plan, the code and this handoff. CHARTER §6 and Principle 7 want CP6 refine and CP7 verify on a different agent, and the owner has not asked for subagents this session. CP6/7/8 are therefore open, and named as such below."
findings_summary: "GREEN, AND THE GREEN WAS NOT ENOUGH — three real gaps shipped past a fully green suite, all of the same shape: the service and the REST surface carried a behaviour and no screen reached it, while the scenario that pinned the behaviour was `@backend` and bound at the services layer. (1) THE LINK HAD NO PICKER: a purchase could be pointed at a meta from Python and from nowhere a browser could reach; `TransactionCreate` carried no `meta_id` at all. (2) THE BREAKDOWN NEVER LEFT THE SERVER: `MonthAvailableOut` declared five fields and dropped the metas, contributions and releases the fold had been computing, so /reports showed a column that did not add up — 5.000.000 − 100.000 − 6.400.000 read as −4.100.000. AC-4 asks for that breakdown in as many words. (3) THE SAVINGS VIEW DID NOT EXIST: `funds.month_split`, `MonthSplitOut`, `GET /metas/split` and `lib/api/metas.monthSplit` were all shipped and nothing called them — the thing the owner asked for in his own words. A FOURTH WAS WORSE BECAUSE IT LOOKED DONE: the /metas screen's three buttons rendered with no `onClick`, and the screen test asserted they existed without clicking one. Cancelling, restoring and contributing had no way in either, and restoring was unreachable even in principle because no endpoint listed a cancelled meta. ALL FOUR WERE FOUND BY DRIVING THE APP IN CHROME AGAINST A REAL SERVER, NOT BY THE SUITE. The lesson for CP6/7 is specific: a feature whose screen scenarios are thin and whose behaviour scenarios are all `@backend` will report green over an unusable app."
human_action_needed: yes
human_action_kind: decision
recommended_next: "Three things, in order. (1) THE OWNER RUNS `just backup && just migrate` — 0014, 0015 and 0016 are outstanding and CHARTER §7 puts migrations touching real data in his hands. Production is clear for 0015: `SELECT rule, COUNT(*) FROM fund` returns 0 rows, checked read-only 2026-08-08. (2) CP6 refine ON A FRESH AGENT — this one wrote the code and cannot verify it (Principle 7). (3) CP7 crap-analyzer and CP8 mutation, also fresh; plan.md's Test strategy opts mutation in on exactly domain/rules.py and services/metas.py."
tracker_update: "local — 009 at checkpoint 5, status in-progress. 125 scenarios bound, 0 unbound. Backend 1010, vitest 397, lint clean. Migrations 0014/0015/0016 outstanding and human-owned. CP6/7/8 open and blocked on a fresh agent."
status: complete
---

# implement — 009 named-goals

Escrito en retrospectiva junto con el de CP4. El código estaba; el rastro no.

```
009        125 escenarios · unbound 0
010        unbound 0
backend    1010 passed
vitest     55 archivos · 397 passed
lint       limpio
consultas  14, y la prueba lo defendió tres veces
```

## Lo que el verde no vio

Cuatro huecos, todos de la misma forma: **el servicio lo hacía, la pantalla no
lo alcanzaba**, y el escenario que lo fijaba era `@backend`.

```
el vínculo          existía en services, no en el cable ni en la pantalla
el desglose         el servidor lo calculaba, el esquema lo botaba
la vista de ahorro  servicio + endpoint + cliente listos, nadie los llamaba
los tres botones    renderizaban sin onClick, y la prueba solo los miraba
```

Los cuatro salieron **manejando la app en Chrome**, no de la suite.

## Lo que queda, y de quién es

```
0014 0015 0016   del dueño        just backup && just migrate
CP6 refine       agente fresco    quien escribió esto no puede verificarlo
CP7 crap         agente fresco
CP8 mutación     agente fresco    rules.py y metas.py, según plan.md
```
