---
skill: close-findings
agent_id: main-session
feature: 009-named-goals
started: 2026-08-09T1645
ended: 2026-08-09T1745
checkpoint: 5
artifacts:
  - features/009-named-goals/handoffs/2026-08-09T1745-close-round3.md
exit_criteria:
  - criterion: Every acceptance scenario is bound and green
    verified_by: tool
    met: true
    evidence: "`./run-acceptance-tests.sh` → `484 passed, 1 warning in 28.30s`. Twelve scenarios were added to `spec.md` with the owner's approval after this round closed — 009 goes from 125 to 137. THE FIGURE BEFORE THEM WAS 472 AND HAD NOT MOVED IN THREE ROUNDS: 472 green scenarios were blind to twenty-eight wrong ACs. Eleven of the twelve go red against the code they were written to catch; the twelfth is the control arm of AC-8's pair."
  - criterion: Every AC is genuinely reachable and correct
    verified_by: judgment
    met: true
    evidence: "The six wrong and the three partial are closed, each pinned by a named test that fails against the prior code. AC-15 (cancelling a meta contributed to gave the contribution back twice), AC-36 (the report totalled 0 over a table listing 1.600.000), AC-16/AC-29 (a meta finished by lowering never left the screen once closed), AC-43 (`POST /planned` dropped `meta_id`, and Planear pago had no selector), AC-25 (the refusal said cancelled when the meta was closed), AC-4/AC-31 (a finished meta took a $0,00 line in the breakdown for ever). ONE REPORT IS REBUTTED RATHER THAN CLOSED — see findings_summary. AS IN BOTH PRIOR ROUNDS, THIS CRITERION IS ASSERTED BY THE AGENT THAT WROTE THE CODE."
  - criterion: The unit suite is green
    verified_by: tool
    met: true
    evidence: "`cd backend && SESSION_SECRET=… uv run pytest -q` → `1082 passed, 1 warning in 46.51s`, up from 1074. Six service tests for the money-minting, the report total and the closed-after-lowering meta; two HTTP round-trips for the planned link and the refusal wording."
  - criterion: The frontend suite is green
    verified_by: tool
    met: true
    evidence: "`cd frontend && pnpm vitest run` → `Test Files 55 passed (55)`, `Tests 421 passed (421)`, up from 420. One on the plan form's meta selector."
  - criterion: The lint and boundary gates are green
    verified_by: tool
    met: true
    evidence: "`just lint` → exit 0 after a `ruff format` on the new tests. `Contracts: 2 kept, 0 broken`."
  - criterion: Mutation holds on the module that changed
    verified_by: tool
    met: true
    evidence: "`scripts/mutate.py --target backend/src/quaestor/services/metas.py` over the four-stage ladder, now with the twelve new acceptance scenarios in stage 3: **169 mutants, 162 killed, 7 survived — 95.9% raw, 100% adjusted.** THE SWEEP CHANGED THE CODE RATHER THAN ONLY THE TESTS. Six of its first eleven survivors sat in `_finished_by_now`, a second fold added to ask whether a meta had finished while the first fold already walked the same months and threw the answer away; `_walk` now returns both and the six sites are gone by construction. A seventh named a real defect: the before-the-start branch could report a meta as finished with nothing noticing, because the only test over that branch asserted the meta WAS listed in a month that ended before it existed — a test written earlier in this same session to kill a different mutant. THAT IS THE THIRD TIME IN THIS FEATURE A MUTANT-KILLING TEST PINNED WRONG BEHAVIOUR (`posted_only`, the amendment month, and this). Three more were killed by naming figures rather than shapes: the fold opens at zero when the owner stated nothing, and the release that finishes a meta is any release — one centavo below what it holds — in the last month and in every month before it. The seven left are equivalent: `frozen=True` on three dataclasses nothing mutates, `opening > amount` -> `>=` whose equal case produces identical figures down every branch, the before-the-start `finished` flag that no caller now reads, and twice `else 0` on a progress percentage an amount validation never lets reach zero."
  - criterion: Migrations rehearsed before any touch real data
    verified_by: human
    met: true
    evidence: "APPLIED BY THE OWNER ON 2026-08-09, not by me. `just backup && just migrate` behind a 53K dump written at 15:07 — the recipe refuses without a dump dated today, so ADR-0030's rule was enforced as well as followed. `alembic upgrade head` took the schema 0013 → 0016 in one go, so 0014 and 0016 landed with the destructive 0015. Verified read-only afterwards: `alembic_version` = 0016; the `fundrule` enum is `fixed, average, from_recurring` with `target_by_date` gone; `meta`, `meta_contribution` and `meta_amendment` all exist and `meta` carries `stated_opening`, `closed`, `archived`, `cancelled_month`; and every count is what the 0013 check recorded — 635 movements, 7 accounts, 43 categories, 0 funds, 14 recurring items. `fund.anchor_month`/`anchor_amount` remain and should: they are product ADR-041's statement of what a fund already holds, not the dated rule's."
  - criterion: Verification independence (Principle 7)
    verified_by: judgment
    met: false
    evidence: "NOT MET AND NOT WORKED AROUND. `agent_id: main-session`, for the fourth time on this checkpoint. `dae_handoff.py --through 5` still exits 1 on `CP6 handoff 2026-08-09T0758-refine.md shares agent_id main-session with the CP5 implement handoff`."
findings_summary: "THE APP WAS MINTING MONEY, AND THAT ONE IS OLDER THAN ANY FIX THIS SESSION MADE. Cancelling a meta the owner had put money into by hand handed that money back without ever having stopped charging it: the month a meta is cancelled in drops it from `agg.metas`, so `contributed_total` no longer counted the contribution while `_released_by` — which folds the meta's holdings — kept returning it. An 8.000.000 meta with 2.000.000 contributed in October took October's disponible from 1.400.000 to 10.200.000. AC-15's own arithmetic gives 8.200.000. The breakdown printed the give-back with no `Puesto a mano en una meta` row above it and the split read `ahorro −5.200.000`. Product ADR-014 forbids exactly this by name. THE REPORT TOTALLED ZERO OVER A TABLE LISTING 1.600.000: `asked` summed the live metas while the section listed the cancelled ones too — mine, from round one. A META FINISHED BY LOWERING ITS AMOUNT NEVER LEFT THE SCREEN once closed: AC-16 completes a meta with no purchase at all, and `_gone_from_the_list` read the finish off the purchase alone — mine, from round two, and the only one of the six that a previous fix caused. The movement dialogs offered it and the server refused it saying it had been CANCELLED, which it had not. AND AC-43'S LINK STILL NEEDED TWO STEPS: `PlanPaymentIn` carried no `meta_id`, the router forwarded none, and Planear pago had no selector — round one reported this and I did not fix it then. ONE REPORT IS REBUTTED. Round three said AC-16's own figures do not reproduce: lowering an 8.000.000 meta to 4.000.000 while it holds 4.800.000 would leave it running. THEY DO REPRODUCE — it depends which month the edit is made in, and AC-16 says which: *a meta HOLDING 4.800.000*. Edited in November, where it opens with 4.800.000, it releases 800.000 and completes; edited in October, where it opens with 3.200.000, it does not. The verifier edited in October. `tests/api/test_metas.py::test_lowering_a_meta_below_what_it_holds_completes_it_over_the_wire` pins November and passes. Nothing was changed for it."
human_action_needed: yes
human_action_kind: decision
recommended_next: "The owner decides. (1) THE TWELVE SCENARIOS ARE IN — he reviewed them and approved them after this handoff was first written, and `spec.md` now carries 137. Eleven of the twelve go red against the code they were written to catch; the twelfth is AC-8's control arm and must pass on both sides. (2) WHETHER A FOURTH VERIFIER RUNS. The trend is 13, then 9, then 6 — decreasing, and not yet zero, and round three's worst finding was older than any fix this session made rather than caused by one. (3) `just backup && just migrate` for 0015 and 0016, then the merge — charter §7. CP6, CP7 and CP8 have now all run against code that has changed three times since."
tracker_update: "local — 009 stays at checkpoint 5, status in-progress. Round three's six findings and three partials closed; one report rebutted with its reproduction. Backend 1074 → 1082, vitest 420 → 421, acceptance 472 → 484 after the owner approved twelve new scenarios — 009 goes from 125 to 137. Migrations 0015/0016 still outstanding and human-owned."
status: complete
---

# close-round3 — 009 named-goals

El tercer verificador encontró seis ACs mal y tres a medias. Están cerrados, y
uno de sus reportes está **rebatido**, no cerrado.

```
backend      1082 passed   (era 1074)
vitest       55 archivos · 421 passed   (era 420)
aceptación   484 passed    (eran 472, ciegos por tres rondas)
lint         exit 0 · contratos 2 kept, 0 broken
```

## La app estaba inventando plata

Y esta es más vieja que cualquier arreglo de esta sesión.

```
meta $8.000.000 para diciembre, abierta en agosto, ingreso $5.000.000
le pones $2.000.000 a mano en octubre     disponible 1.400.000
cancelas la meta en octubre               disponible 10.200.000
la aritmética del propio AC-15 da          8.200.000
```

El mes en que se cancela una meta la saca de `agg.metas`. Así
`contributed_total` dejaba de contar el aporte — pero `_released_by`, que dobla
lo que la meta llevaba, seguía devolviéndolo. Dos millones inventados. El
desglose imprimía el *devolvió* sin la fila *Puesto a mano en una meta* encima,
y el reparto leía `ahorro −5.200.000`. Producto ADR-014 lo prohíbe por su
nombre.

## El reporte sumaba cero sobre una tabla que decía $1.600.000

`asked` sumaba las metas vivas; la sección listaba también las canceladas. Mío,
de la primera ronda.

## Una meta cerrada sin compra no salía nunca de la pantalla

AC-16 termina una meta **sin compra ninguna**: bajarle el monto por debajo de lo
que ya tenía libera el exceso. `_gone_from_the_list` leía el final solo de la
compra. Mío, de la segunda ronda, y el único de los seis que causó un arreglo
anterior. Los diálogos de movimiento la ofrecían y el servidor la rechazaba
diciendo que había sido *cancelada*, que no lo fue.

## Y AC-43 seguía necesitando dos pasos

`PlanPaymentIn` no tenía `meta_id`, el router no lo pasaba, y *Planear pago* no
tenía selector. La ronda 1 lo reportó y no lo arreglé entonces.

## Lo que rebato

La ronda 3 dice que las cifras de AC-16 no reproducen. **Sí reproducen** —
depende del mes en que edites, y AC-16 dice cuál: *"a $8.000.000 meta **holding
$4.800.000**"*.

```
editada en noviembre   abre con 4.800.000 → libera 800.000 · complete
editada en octubre     abre con 3.200.000 → no libera nada · sigue corriendo
```

El verificador editó en octubre. `test_lowering_a_meta_below_what_it_holds_completes_it_over_the_wire`
lo fija en noviembre y pasa. No cambié nada por esto.

## Lo que la suite de aceptación sigue sin ver

Veintiocho ACs mal en tres rondas, y 472 escenarios verdes en todas. Hay doce
escenarios redactados con cada cifra corrida, esperando tu revisión. `spec.md`
es tu contrato y ningún agente lo ha tocado.

## Human action needed?

**Sí — decisión.** Los doce escenarios ya están dentro: el dueño los aprobó
después de escribirse este handoff, y `spec.md` lleva 137. Queda si corre una
cuarta ronda, y las migraciones y el merge, que son suyos.
