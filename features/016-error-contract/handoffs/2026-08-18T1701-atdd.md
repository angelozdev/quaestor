---
skill: engineer:atdd (bridging atdd:atdd)
agent_id: main-session
feature: 016-error-contract
started: 2026-08-18T1537
ended: 2026-08-18T1701
checkpoint: 3
artifacts:
  - features/016-error-contract/spec.md
  - features/016-error-contract/.build/spec.json
  - features/016-error-contract/.build/generated/test_016_error_contract.py
  - acceptance/handlers/error_contract.py
  - acceptance/handlers/__init__.py
findings_summary: "9 scenarios (5 @backend, 4 untagged/vitest) from 6 ACs; AC-6 deliberately carries zero Gherkin scenarios — agreed with the owner during this checkpoint (2026-08-18) that pinning an injected-fault path would mean building fault-injection infrastructure this suite has never needed, so a backend unit test on the exception handler covers it instead. AC-1/AC-2 reuse feature 008's existing Given/When steps and its `the registration is rejected` handler verbatim, adding only the two new Spanish/code Then steps each needs. AC-3 reuses feature 012's correction steps. AC-4 goes through the real REST boundary (POST /api/funds) rather than the service layer, because the AC is specifically about Pydantic's own RequestValidationError crossing the wire — a domain-level call would bypass the exact mechanism under test. AC-5 reuses feature 005's TRM/report steps and is a regression guard: it pins today's raw English MissingRate text verbatim, since that site is deliberately not migrated by 016. spec-guardian found one implementation leak (a step self-referencing 'AC-1' instead of describing the observable difference) — fixed with the owner's explicit approval, spec.md re-parsed and pytest regenerated after the fix, same 5-failed/1-passed result confirmed independently (not just on the subagent's word). Also flagged and resolved as non-issues: three 'Then the Name/Amount field shows...' steps, which spec-guardian checked against acs.md and found match the owner's own approved framing verbatim. The atdd:pipeline-builder agent's work was independently re-verified rather than taken on trust: reran the 016 suite myself (same 5 failed/1 passed, zero unsupported-step errors), ran ruff check+format on both changed handler files (clean), and reran 008/012/005/007's full generated suites together (211 passed, no regressions) since 016's new handlers reuse steps from all four."
human_action_needed: no
recommended_next: "/engineer.plan"
tracker_update: null
exit_criteria:
  - criterion: "spec.md parses to a valid IR"
    verified_by: tool
    met: true
    evidence: "dae_gherkin.py features/016-error-contract/spec.md features/016-error-contract/.build/spec.json — exit 0, produced background/name/scenarios with 9 scenarios, re-run clean after the spec-guardian fix"
  - criterion: "every AC maps to >=1 scenario"
    verified_by: inspection
    met: true
    evidence: "AC-1..AC-5 each have >=1 @backend scenario plus (AC-1/2/3) an untagged UI scenario — 9 total. AC-6 is the documented, owner-agreed exception: covered by a backend unit test instead of a Gherkin scenario (see findings_summary); spec.md states this explicitly under 'AC-6 has no Gherkin scenario, on purpose' rather than silently omitting it."
  - criterion: "spec-check passes"
    verified_by: tool
    met: true
    evidence: "atdd:spec-guardian reviewed all 48 Given/When/Then/And statements across 9 scenarios: 1 clear-cut leak (self-reference to 'AC-1' in a Then step) found and fixed with owner approval; 3 flagged field-name mentions checked against acs.md's owner-approved wording and confirmed as intentional domain framing, not leakage. Re-verified by inspection after the fix (one-line reword, regex updated to match in acceptance/handlers/error_contract.py, IR and generated pytest regenerated)."
  - criterion: "generated pytest runs (not just parses) against real service/API calls, with red for the right reason"
    verified_by: tool
    met: true
    evidence: "cd backend && uv run pytest ../features/016-error-contract/.build/generated/ -q, run independently by this session (not only reported by the pipeline-builder agent): 5 failed, 1 passed. Zero 'unsupported step' errors. Failures are genuine — e.g. AC-4 got 'category_id: field required' (real Pydantic English) where a Spanish message was asserted; AC-1/AC-2's code assertions fail because domain exceptions carry no `code` attribute yet. AC-5's single pass is a legitimate regression-guard pass, not a stub: the site is meant to be unchanged by this feature, and it is."
  - criterion: "no regressions in suites this feature's handlers reuse steps from"
    verified_by: tool
    met: true
    evidence: "cd backend && uv run pytest ../features/008-mandatory-categories/.build/generated/ ../features/012-movement-corrections/.build/generated/ ../features/005-fx-read-time-conversion/.build/generated/ ../features/007-recurring-engine/.build/generated/ -q — 211 passed"
  - criterion: "the owner has reviewed and approved spec.md"
    verified_by: human
    met: true
    evidence: "«apruebo, sigue con el pipeline», 2026-08-18, after spec.md was presented with the AC-4/AC-6 notes and the 008 cross-feature flag; the one spec-guardian fix was separately approved: «sí, aplica el cambio y continúa»"
status: complete
---

# atdd (CP3) — resumen del handoff

**9 escenarios, 5 `@backend`, 4 sin tag (vitest).** AC-6 sin escenario Gherkin
a propósito — un test unitario del manejador cubre "un bug revienta a mitad
de una petición" sin inventar infraestructura de falla inyectada.

## Lo que se reutilizó, en vez de reinventarse

AC-1/AC-2 reutilizan los pasos Given/When de la 008 tal cual, y su paso `the
registration is rejected` — este módulo solo agrega los Then nuevos de
español/código. AC-3 reutiliza los de la 012. AC-5 reutiliza los de la 005 y
es un **guardia de no-regresión**: fija el texto en inglés de hoy, porque ese
sitio queda deliberadamente sin migrar.

## La fuga que encontró spec-guardian, y lo que no era fuga

Un paso decía *"distinct from AC-1's"* — se refería al número del propio
documento, no a algo observable desde afuera. Corregido con tu aprobación a
*"distinct from the code for an active-category duplicate"*, con el handler
y la IR regenerados después. Los tres pasos que nombran el campo ("Name
field", "Amount field") **no son fuga** — se revisaron contra `acs.md` y
coinciden con tu propia redacción («bajo el campo Nombre», «bajo el campo
Monto»).

## Verificado en persona, no solo reportado

El agente `pipeline-builder` construyó los handlers; antes de darlo por
bueno, esta sesión corrió el suite de la 016 de nuevo (mismo resultado: 5
fallan, 1 pasa, cero pasos sin handler), `ruff` sobre los dos archivos
tocados, y el suite completo de las cuatro features de las que 016 toma
pasos prestados (008, 012, 005, 007 — 211 pasan, ninguna regresión).

```
DAE ▸ 016-error-contract
·0 Onboard · ·1.5 Ready · ·2 ACs · ▶3 Spec · ·4 Plan · ·5 Implement · ·6 Refine · ·7 Verify · ·8 Harden
```
