---
skill: engineer:consistency-check + crap-analyzer:crap-analyzer
agent_id: cp7-cp8-independent-verifier
feature: 016-error-contract
started: 2026-08-19T0825
ended: 2026-08-19T0927
checkpoint: 7
artifacts:
  - features/016-error-contract/feature.md (read only)
  - features/016-error-contract/acs.md (read only)
  - features/016-error-contract/spec.md (read only)
  - features/016-error-contract/plan.md (read only)
  - features/016-error-contract/.build/spec.json (read only)
  - docs/adr/0059-un-error-de-dominio-lleva-un-codigo-estable-y-sus-datos-la-copia-en-espanol-vive-en-el-cliente.md (read only)
  - backend/src/quaestor/domain/errors.py (read only)
  - backend/src/quaestor/api/errors.py (read only)
  - backend/src/quaestor/services/categories.py (read only)
  - backend/tests/api/test_errors.py (read only)
  - frontend/lib/api/error-catalog.ts (read only)
  - frontend/lib/api/client.ts (read only)
  - frontend/components/entity-form-dialog.tsx (read only)
  - frontend/app/(app)/categories/page.tsx (read only)
exit_criteria:
  - criterion: "feature.md/acs.md/spec.md/plan.md agree with each other and with the code as it stands now"
    verified_by: tool
    met: true
    evidence: "Ran the consistency-check skill's checklist by hand against every artifact (no project-specific automation beyond dae_resolve.py / dae_handoff.py existed to invoke). feature.md slug matches folder; frontmatter complete (status: ready, autonomy_level: medium); acs.md AC-1..AC-6 unique/sequential, ac_count: 6 and high_priority_count: 4 both verified against the actual ACs; acs.md ACs read in domain language, no implementation leakage; acs.md covers every thread of feature.md's outcome; relevant_adrs: [0001] and ADR-0059 both exist and are accepted; spec.md's 9 scenarios match .build/spec.json's 9 exactly (spot-checked every scenario name/step); the 6 @backend scenarios map 1:1 to the 6 generated pytest functions in .build/generated/test_016_error_contract.py; the 3 untagged scenarios are bound by exact name in vitest (grep-confirmed in categories/page.test.tsx x2, transaction-edit-dialog.test.tsx x1); plan.md's Charter Check has no unresolved ⚠️; feature.md's validation_method is referenced by plan.md's Test strategy section; dae_handoff.py reports 'ok -- latest complete checkpoint: 6', no missing/interrupted handoffs. `uv run lint-imports` re-run independently: 'Contracts: 2 kept, 0 broken', confirming refine's fix is still in place."
  - criterion: "AC-6 (no Gherkin scenario, per CP3's decision) is pinned by a real backend unit test that exercises the catch-all Exception handler"
    verified_by: tool
    met: true
    evidence: "backend/tests/api/test_errors.py::test_unexpected_exception_answers_with_a_fixed_spanish_code_and_message and ::test_unexpected_exception_is_logged_but_never_reaches_the_client both drive `/boom/unexpected` (a RuntimeError subclass distinct from every QuaestorError), assert the response is {error: internal_error, detail: fixed Spanish message}, assert the real exception type and message ARE captured in the log via caplog, and assert the real exception text never appears in the response body. Read in full; genuinely exercises `_unexpected`, not a stub."
  - criterion: "CRAP analysis ran over methods this feature's diff touched or added, with particular attention to _spanish_for/_PYDANTIC_ES and the catch-all handler"
    verified_by: tool
    met: true
    evidence: "compute_crap.py run against a git-diff of every changed file (untracked new files included via `git add -N` + `git reset` to avoid staging content) using real vitest lcov coverage (3 touched frontend test files) and coverage=n/a for backend (coverage.py could not run in this environment — reproducible `ImportError: cannot import name claw_state from partially initialized module beartype.claw._clawstate` the instant pytest-cov's tracer is active; confirmed unrelated to this feature by running the same tests with no coverage instrumentation, which pass cleanly). Backend n/a coverage compensated with manual test-tracing: `_spanish_for` has 10 direct unit tests (one per _PYDANTIC_ES key plus the unmapped-type fallback); `_domain_body`'s data/no-data branches both exercised at the real API layer; `_format_validation` exercised end-to-end by AC-4's acceptance scenario. 15 findings above threshold 10, reported worst-first with a real/pre-existing-complexity distinction for each (see findings_summary)."
  - criterion: "plan.md's Architecture section checked against the code as it stands after refine's require_positive relocation, and the drift characterized"
    verified_by: inspection
    met: true
    evidence: "Read plan.md's 'La limpieza que hace literal una sola vez, cinco puertas' section: its pseudocode has require_positive staying in transactions.py, called by recurring/metas/funds.py FROM there — the exact design refine's lint-imports run found to violate the metas/funds layering contract. Confirmed via source read that the current code has require_positive in domain/errors.py, imported directly by all four call sites, and via a fresh `uv run lint-imports` that both contracts are kept. Characterized as real drift, not neutral evolution (plan.md documents a design that was actually wrong, not one that merely aged) — reported as a finding for the plan.md addendum, not edited (no instruction authorizes editing plan.md; independence means reporting, not silently patching)."
findings_summary: "Independent verification (fresh read, no assumptions carried from the implement/refine handoffs beyond using them as pointers to check). CONSISTENCY: everything holds — no schema violations, no cross-artifact drift beyond one finding (below). The 6 @backend acceptance scenarios and AC-6's unit test cover exactly the surface acs.md and spec.md promise; the 008/009 handler fixes documented in the implement handoff are in place and correct (then_told_category_exists/then_offered_to_restore now check .code, named_goals's then_told_amount checks the new require_positive message). FINDING 1 (plan.md drift, not blocking): plan.md's Architecture section pseudocode still shows require_positive living in transactions.py — the design refine (CP6) found broken via lint-imports and fixed by relocating it to domain/errors.py. A future reader trusting plan.md's code block literally would reintroduce the exact bug refine fixed. Recommend a one-line addendum to plan.md pointing at the refine handoff; not applied here (independence — report, don't silently patch someone else's artifact). CRAP: the two functions the task specifically named (_spanish_for, the catch-all _unexpected handler) both have solid, essentially branch-complete manual-traced coverage despite showing as 0%/n/a to the automated tool (an environment limitation, not a code-quality signal) — real risk on both is LOW. Every backend function flagged above the CRAP threshold that ISN'T new to this feature (_apply_edit, _validated_spec, transfer, create_recurring, _validate_spec, contribute) is pre-existing complexity this feature touched with exactly one line each (replacing an inline check with a call to the shared require_positive) — not new risk. FINDING 2 (the most concrete, actionable gap found): the `data` field's actual wire-level round trip — backend `_domain_body` serializes `exc.data` into the JSON response's `data` key, frontend `client.ts`'s interceptor reads `data?.data` and constructs `ApiError` with it — had ZERO end-to-end test coverage anywhere in the diff at the time this analysis was performed. Every layer was tested in isolation with hand-built objects (backend tests read `exc.data` on the raised Python object directly, never decode a real JSON response and check `resp.json()['data']`; frontend page-level tests mock `createCategory`/`updateTransaction` directly and hand-construct `ApiError` objects, bypassing `client.ts`'s interceptor entirely). Empirically confirmed as a real, non-equivalent gap during the CP8 harden pass (see that handoff for the mutation proof) — and independently closed mid-session by the repo owner's own concurrent edit to `client.test.ts`, not by this checkpoint. FINDING 3 (minor): `error-catalog.ts`'s `category_duplicate_archived` translator is tested only for the expense direction; the parallel `category_duplicate_active` translator is tested both directions (matching AC-1's explicit 'El mismo patrón aplica a una categoría de ingreso' — AC-2 makes no equivalent claim, so this is arguably in-spec, but the asymmetry is real and traced precisely via lcov branch data, not assumed). FINDING 4 (minor): `frontend/app/(app)/categories/page.tsx`'s onOpenChange callback (clears nameFieldError when the create-category dialog closes) has no direct test. No code was written to close any of these — reported per the task's independence mandate."
human_action_needed: "no (informational findings only; nothing blocks merge)"
recommended_next: "CP8 harden (mutation testing) — see the companion handoff. Separately, optional: add a one-line addendum to plan.md's Architecture section noting the require_positive relocation (Finding 1), and consider a small end-to-end test for the data wire round-trip if the externally-added client.test.ts coverage (Finding 2) is ever reverted or refactored away."
tracker_update: null
status: complete
---

# verify (CP7) — resumen del handoff

**Consistencia limpia salvo un detalle real en `plan.md`, y el análisis CRAP
encontró el hueco más concreto de toda la feature: nada prueba de punta a
punta que el campo `data` cruza el cable.**

## Consistencia

Los seis escenarios `@backend` y el test unitario del AC-6 cubren exactamente
lo que prometen `acs.md` y `spec.md`. Los arreglos a los handlers de la 008 y
la 009 documentados en el handoff de implementación siguen en su lugar.

**Un hallazgo real, no bloqueante**: `plan.md` todavía describe, en su
pseudocódigo, el diseño de `require_positive` viviendo en `transactions.py` —
exactamente el diseño que el `refine` (CP6) encontró roto por `lint-imports` y
corrigió moviéndolo a `domain/errors.py`. Alguien que confíe en `plan.md` tal
cual está podría reintroducir el mismo bug. No lo edité — solo lo reporto.

## CRAP

Las dos funciones que se pidió mirar con cuidado (`_spanish_for` y el manejador
`_unexpected`) están bien cubiertas, aunque la herramienta automática las
mostró en 0% por un problema del entorno (coverage.py no corre junto a los
hooks de importación de beartype en esta máquina — confirmado, no es un
problema del código). Rastreado a mano: cobertura real alta en ambas.

**El hallazgo que más importa**: nada en el diff prueba que el campo `data`
realmente cruza el cable — el backend lo serializa, el cliente lo lee, pero
cada lado se prueba con objetos armados a mano, nunca los dos juntos. Se
confirmó como hueco real (no equivalente) durante el CP8, y se cerró a mitad
de esta sesión por un cambio externo del dueño, no por este checkpoint.

```
DAE ▸ 016-error-contract
·0 Onboard · ·1.5 Ready · ·2 ACs · ·3 Spec · ·4 Plan · ·5 Implement · ·6 Refine · ▶7 Verify · ·8 Harden
```
