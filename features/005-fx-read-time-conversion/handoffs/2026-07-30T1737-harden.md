---
skill: harden
agent_id: architect-1
started: 2026-07-30T1729
ended: 2026-07-30T1737
checkpoint: 8
artifacts:
  - backend/tests/domain/test_money.py (+1 test: implied_rate accepts one-cent legs)
  - backend/tests/services/test_fx.py (+1 test: set_trm accepts rates at or below one)
  - backend/tests/services/test_transactions.py (+5 tests: one-cent transfer legs; list category filter; date_to inclusive boundary; update rejects missing category; delete keeps other transactions' tags)
exit_criteria:
  - criterion: "Mutation testing over the core changed modules per the charter policy (atdd:mutate approach)"
    verified_by: tool
    met: true
    evidence: "No mutation framework in the project and dae_gherkin_mutate.py targets specs, not code — built the minimal AST harness the skill describes (scratchpad, not committed): arithmetic +/-, */ swaps, comparison and is/in swaps, augmented +=/-=, boundary constants 0<->1, ROUND_HALF_UP->ROUND_HALF_DOWN. 65 mutants over domain/money.py (12), services/fx.py (5), services/transactions.py (48); targeted subset per mutant, originals restored after each run"
  - criterion: "Mutation score reported with survivors dispositioned"
    verified_by: tool
    met: true
    evidence: "Targeted pass 53/65 killed (81.5%). All 12 targeted survivors escalated against the FULL backend suite + acceptance run: 3 more killed (category-filter mutant by other suites; 1-cent registration by the AC-14 0.01-USD acceptance scenario), leaving 9 true survivors = 86.2% true score, assessment moderate. Zero survivors revealed a real bug — each weakened a guard no test exercised: implied_rate 1-cent legs (x2), set_trm rate<=1, transfer 1-cent sent/received (x2), list category-filter equality, date_to inclusive boundary, update-with-missing-category validation, delete crossing into other transactions' tag links"
  - criterion: "Genuine survivors killed with targeted unit tests (no behavior locked that is buggy)"
    verified_by: tool
    met: true
    evidence: "7 unit tests added (comment-free, matching each file's conventions); re-ran all 9 surviving mutants -> 9/9 killed. Final score 65/65 (100%). Production code untouched during CP8 — every kill came from a new test asserting already-correct behavior"
  - criterion: "Final full runs all green"
    verified_by: tool
    met: true
    evidence: "./run-acceptance-tests.sh features/005-fx-read-time-conversion -> '26 passed, 1 warning in 1.43s'; cd backend && uv run pytest -> '705 passed, 1 warning in 12.96s' (698 + 7 new); cd frontend && pnpm vitest run -> 'Test Files 36 passed (36) / Tests 197 passed (197)'; pnpm exec tsc --noEmit exit 0. Mutated sources verified restored (diff on the three modules back to the feature baseline; suites green on them)"
findings_summary: "Mutation: 65 mutants, true score 86.2% before hardening, 100% after 7 targeted kill-tests; no real bugs found. All streams green: acceptance 26/26, backend 705, frontend 197. AC-12 remains partial (runbook open). SESSION_SECRET must be exported for the acceptance runner or AC-13 fails spuriously"
human_action_needed: yes
human_action_kind: review + decision
recommended_next: "human: review diff, commit, run runbook migration steps, merge"
status: complete
---

# harden — handoff summary (Checkpoint 8)

## Mutation pass (charter policy, plan.md Test strategy)

Scope: the three core changed modules — `domain/money.py`,
`services/fx.py`, `services/transactions.py`. No mutation framework
exists in the repo and none was installed; a minimal AST-based harness
(mutate one operator at a time, run the module's targeted test subset,
restore, escalate survivors to the full backend + acceptance suites) ran
from the scratchpad and left no files in the repo.

- 65 mutants: 53 killed by targeted subsets, 3 more by the full suites
  (notably: AC-14's 0.01-USD acceptance scenario kills the 1-cent
  registration boundary mutant — the spec is doing real mutation work).
- 9 true survivors, **none a real bug** — production behavior is correct
  in every case; each survivor exposed an untested guard. Dispositions:
  - `implied_rate` positive-amount guard at 1 cent (2 mutants)
  - `set_trm` accepts legitimate rates <= 1 (boundary 0->1)
  - `transfer` sent/received amounts at 1 cent (2 mutants)
  - `list_transactions` category-filter equality and `date_to`
    inclusiveness
  - `update_transaction` missing-category validation
  - `delete_transaction` tag-link cleanup scoped to the deleted row
- 7 comment-free unit tests added (files above); re-run of all 9
  survivors: 9/9 killed. **Final score 65/65 (100%).**

## Final green runs (tool evidence)

- Acceptance: 26 passed (26/26 executions).
- Backend: 705 passed (698 baseline + 7 new kill-tests).
- Frontend: 36 files / 197 tests passed; `tsc --noEmit` clean.

## Notes for the human review

1. **AC-12 is still partial** — the four runbook steps (backup,
   `just dev-prod` migration, TRM pre-load check, smoke) are open and
   human-owned. Nothing in CP7/CP8 closes them.
2. **SESSION_SECRET** — `./run-acceptance-tests.sh` fails the AC-13
   parity scenario when SESSION_SECRET is unset (create_app requires
   >=32 bytes). Export it (any >=32-byte value) before running, or fix
   the runner to set a test-only default.
3. **The branch diff includes an uncommitted CLAUDE.md edit** (adds the
   no-comments rule + acceptance-pipeline docs, mtime 16:31 during the
   implement phase — predates this verification session). Review it
   alongside the code diff; it is a policy file change and should be a
   deliberate commit.
4. Minor cleanups worth a follow-up (from CP7): dead
   `fx_rate=/to_base=` kwargs in tests/services/test_planned.py:326;
   stale future-tense docstrings in acceptance/handlers/fx_read_time.py.
5. No commits were made; no migrations were run against real data;
   `.dev-data/**` untouched.

## Recommended next step

human: review diff, commit, run runbook migration steps, merge.
