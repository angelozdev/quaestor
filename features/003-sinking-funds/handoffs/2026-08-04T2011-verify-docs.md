---
skill: atdd-team
agent_id: architect-cp7-docs
started: 2026-08-04T1935
ended: 2026-08-04T2011
checkpoint: 7
artifacts:
  - docs/adr/0043-a-fund-replaces-the-envelope-and-the-goal-and-its-balance-is-derived.md (accepted; a second `proposed` amendment added)
  - docs/adr/0044-the-monthly-number-is-a-fold-over-the-bounded-month-aggregate-and-its-income-term-reconciles-per-category.md (acceptance WITHHELD, returned to `proposed`; the expense-side gap recorded)
  - docs/adr/README.md (0044's row returned to `proposed`)
  - features/003-sinking-funds/handoffs/2026-08-04T2011-verify-docs.md
exit_criteria:
  - criterion: The DAE artifacts agree with each other and with what was built
    verified_by: judgment
    met: false
    evidence: "Seven findings, one of them material: `plan.md` names revision `0011` as the destructive migration in five places (218, 296, 407, 518, 543) while the code, the tests and `runbook.md` all say `0012` and reserve `0011` for the additive `fund` table the runbook explicitly declares needs no gate. An operator following the plan would gate the wrong revision. Not fixed here — the plan is a signed CP4 artifact and correcting a signed plan is the team lead's call. Full list in the body"
  - criterion: spec-guardian ran independently over spec.md, as Test strategy §5 owes
    verified_by: tool
    met: true
    evidence: "Dispatched to the `atdd:spec-guardian` agent read-only over the 92 scenarios / 612 step lines. Result: **zero implementation leakage in any step**. Leakage exists only in four scenario titles (566, 598, 686, 887) and the preamble prose. One blocking finding: AC-7's two scenarios both assert `1200000.00 COP`, which is exactly what an untouched fund asks (`7200000 / 6`), so the AC titled *A fund that gets drained raises its ask* is satisfied by an implementation that ignores draining entirely. No file was modified"
  - criterion: ADR-0043 reads true against what was built, and its status is decided
    verified_by: judgment
    met: true
    evidence: "Accepted, and left accepted. Verified from the six handoffs, not from source (the mutation sweep owns the two files). Hard-delete boundary: confirmed as built — `delete_fund` in services (F0), `DELETE /funds/{id}` and the MCP `delete_fund` in the `write-destructive` tier (F3), AC-21 requiring the fund be deleted before the category is archived, and ADR-0005's goal clause carrying its `Superseded by:`. Fold cost: confirmed still true — the refiner measured 156 `due_dates` calls for a 6-fund x 12-obligation 12-month fold (halved from 312) and 23 ms against a 50 ms budget with obligations dating from 2018. The anchor amendment stays `proposed` and the `Accepted:` line says so explicitly"
  - criterion: ADR-0044 reads true against what was built, and its status is decided
    verified_by: judgment
    met: false
    evidence: "**Acceptance withheld — returned to `proposed`.** The withdrawn lazy-TRM amendment is sound and agrees with `product-decisions.md` § ADR-038 clause by clause, and no residual lazy claim survives anywhere in the file. But the `uncovered` term does not describe what was built: it counts a recurring charge at its promise rather than at what posted (bug 1), which contradicts AC-9's own words and this ADR's own second decision driver. Accepting it would have signed a description the code contradicts in the direction that gets spent"
  - criterion: Coverage / CRAP measured for the changed surface
    verified_by: tool
    met: false
    evidence: "**Deferred, not attempted.** `backend/scripts/mutate.py` (PID 84534, alive since 14:19) is rewriting `domain/rules.py` and `services/funds.py` in place one AST node at a time. Any coverage or CRAP number taken now measures mutated source and would be fiction. Runs once the sweep finishes"
  - criterion: Mutation testing over domain/rules.py and services/funds.py (plan Test strategy §4)
    verified_by: tool
    met: false
    evidence: "**Deferred — the sweep is still running and is the charter-mandated Checkpoint 8 work.** It was launched by a previous attempt and is ~110 minutes in. Its result lands in `backend/.build/mutation-003.json`. Not read, not killed, not disturbed"
  - criterion: The running mutation sweep was not disturbed and no source was touched
    verified_by: tool
    met: true
    evidence: "`git status --short` shows changes only under `docs/` and `features/`. No test suite was run — not `./run-acceptance-tests.sh`, not pytest, not vitest, not `just lint`, not crap-analyzer. Neither mutated file was read, formatted or edited; PID 84534 was left alive. `services/funds.py` is untracked, so no `git show HEAD:` baseline for it exists and every claim about it here comes from the handoffs"
findings_summary: "The document half of CP7 is done; the measured half is not, and is not claimed. spec-guardian: zero step-level leakage, one blocking scenario defect (AC-7 unfalsifiable). ADR-0043 accepted with two `proposed` amendments carved out. ADR-0044 acceptance WITHHELD over bug 1. Seven consistency findings, the material one being that plan.md names the wrong revision as destructive. TWO ESCALATIONS: a concurrent Claude session in this repo flipped ADR-0044 to `accepted` mid-analysis and I reverted it; and the on-track/behind badge the UI renders has no AC behind it at all."
human_action_needed: yes
human_action_kind: decision
recommended_next: "Team lead first, owner second. Team lead: the concurrent-session collision, the plan's 0011/0012 error, and which unit of work each bug gets. Owner: the two ADR-0043 amendments, the on-track/behind definition, and the AC-7 spec repair — all three need permission to edit an approved spec.md. Then CP8: read the sweep's mutation-003.json and run coverage/CRAP on a clean tree."
tracker_update: "local — roadmap `sinking-funds` stays in-progress. CP7 partially complete: documents verified, measurements deferred. Four consolidation rows proposed by the refiner remain unfiled."
status: partial
---

# verify (mitad de documentos) — handoff

## Sobre la autoría y el alcance

Mío, de primera mano. No escribí una línea de esta feature: leí los seis
handoffs, el plan, los ACs, el spec, el charter y las dos ADRs **como
afirmaciones que hay que probar**, no como contexto.

Y el alcance fue deliberadamente angosto, porque dos intentos anteriores de la
fase completa se murieron. **Hice la mitad de documentos y nada más.** Lo que no
hice está abajo, con su razón.

## Primero, dos cosas que el team lead tiene que saber ya

### 1. Hay otra sesión de Claude editando estas mismas ADRs, ahora mismo

A mitad de mi análisis, la ADR-0044 pasó de `proposed` a `accepted` **sin que yo
la tocara**. Mi edición falló con *"file has been modified since read"*, la
releí, y el encabezado ya decía `accepted` con una línea `Accepted:` redactada
por otro. La fila del `README.md` también.

`ps` muestra varias sesiones de Claude vivas en este repo
(`claude --dangerously-skip-permissions -c`, PID 799, entre otras). Alguna está
corriendo la misma tarea de F4.

**Qué hice:** apliqué mi criterio **una sola vez** —la devolví a `proposed` con
la razón escrita dentro del propio archivo— y **no voy a entrar en una guerra de
ediciones**. Si esa sesión la vuelve a marcar aceptada, gana la última escritura
y nadie se entera.

**Esto no es un detalle de proceso.** Dos agentes decidiendo el estado de la
misma ADR sin verse es exactamente lo que el Principio 7 existe para impedir, y
el resultado observable es que una ADR quedó aceptada sin que su verificación se
hubiera hecho. **Que el team lead decida quién tiene la pluma.**

### 2. El badge "En camino" no tiene ningún AC detrás

`on track` / `behind` aparece **dos veces en todo el `spec.md`** (líneas 227 y
251) y **cero veces en `acs.md`** y **cero veces en las dos ADRs**. Es un bullet
de *Scope* del `feature.md` —*"Fund health: balance vs. expected accumulation
('insurance: $400k of $1.2M — on track / behind')"*— que el Checkpoint 2 nunca
convirtió en criterio de aceptación.

Pero la pantalla **sí lo pinta** (la tabla de fondos trae la columna estado) y el
reporte mensual **sí lo cuenta** (`FundsSummary.n_behind`). Una promesa que el
producto muestra y que el contrato no fija. Ahí vive el BUG 2 entero.

## `/engineer.consistency-check` — siete hallazgos

No repito los dos que ya están resueltos (la reversión de la D7 y la corrección
fechada de la D9), ni los pasos del runbook sin ejecutar, ni las revisiones sin
aplicar: las tres cosas están **bien** como están.

### C1 — El plan llama `0011` a la migración destructiva. Es la `0012`. (material)

`plan.md` la nombra mal en **cinco lugares**: 218, 296, 407, 518 y 543.

> `plan.md:218` — *"Revisión `0011`: `DROP` de `goal`, `goal_contribution`,
> `budget` y de `transaction.goal_id`"*

Lo que se construyó es lo contrario: la `0011` **crea la tabla `fund`** (F0,
aditiva) y la `0012` es la destructiva (F2). El `runbook.md` ya está corregido
y dice `0012` en todas partes — F2 lo actualizó, el plan no.

**Por qué importa y no es cosmético:** el runbook declara explícitamente que
*"la revisión aditiva `0011` no necesita gate"*. Un operador que siga el plan en
vez del runbook pondría el gate humano sobre la revisión que no lo necesita y
dejaría sin gate la que borra tres tablas. Es la única contradicción de este
lote que puede costar datos.

**No lo corregí:** `plan.md` es un artefacto firmado del CP4 (`plan_status:
approved`). Corregir un plan firmado es del team lead.

### C2 — La D1 del plan describe un ancla que ya no existe así

`plan.md:52` sigue diciendo que el ancla es *"el único **par** guardado: 'el
2027-01 este fondo tenía $149.100'"*. La enmienda de la ADR-0043 volvió
`anchor_month` nullable, y `NULL` significa *"dicho para el mes que se esté
mirando"* — un ancla sin mes **no es un par**. La D1 nunca se actualizó.

### C3 — La salida de escala que el plan promete la desarma la enmienda del ancla

`plan.md`, *Performance budgets*:

> *"La salida, cuando llegue, es acotar la ventana del fold con el ancla — que
> ya existe justamente para poder empezar el fold en un mes arbitrario."*

Con la enmienda, `set_fund(balance=…)` escribe `anchor_month = NULL`. O sea:
**corregir el saldo de un fondo destruye la única manija con la que el plan
pensaba acotar el fold.** `create_fund(opening_balance=…)` todavía fecha su
ancla, así que la salida sobrevive sólo para los fondos que nunca fueron
corregidos.

La ADR-0043 **no** nombra el ancla como mitigación (sólo dice *"will eventually
want a bounded window"*), así que la ADR queda cierta y el que queda mal es el
plan. Es una razón más para que la enmienda siga `proposed`.

### C4 — La D3 del plan y la ADR-0043 dicen "una sola fórmula para las cuatro reglas"

Es la misma frase que el refinador **borró del docstring** de `services/funds.py`
por no describir el código (su hallazgo 14). Sólo dos de las cuatro reglas miran
lo que el fondo tiene.

Es más que una imprecisión de redacción: *"lo que falta"* leído como *objetivo
menos lo guardado* es falso para `fixed` y `average`, y de ahí sale el BUG 2
entero. **Lo dejé registrado dentro de la ADR-0043** como enmienda `proposed`
(abajo), en vez de reescribir la decisión aceptada.

### C5 — El `feature.md` describe un titular que no es el que se construyó

`feature.md:67` — *"Reformulated headline: **normalized income** − Σ funding
rules − unbudgeted pending"*. Lo construido es
`ingreso reconciliado − Σ piden − sin cubrir`, y la normalización se mudó al
**otro** número (`rates().earning`, la AC-14b). Es justo la decisión que el CP2
tomó (decisión 5 de `acs.md`) y que el `feature.md` nunca recogió. Quien empiece
a leer por el `feature.md` se lleva la fórmula equivocada.

Menor, del mismo archivo: `relevant_adrs: [0006, 0028]` no menciona la 0043 ni
la 0044, que son las suyas.

### C6 — La decisión 9 del `acs.md` cita "decisión 5" y apunta al archivo de al lado

`acs.md:142` — *"porque la decisión 5 dice que la app arranca vacía"*. La
decisión 5 **del `acs.md`** es *"Dos números, porque hay dos preguntas"*. La que
dice que la app arranca vacía es la 5 **del `feature.md`**. Referencia cruzada
sin etiqueta.

### C7 — Los conteos por fase del plan no cuadran con lo ejecutado

El plan reparte 48 / 32 / 6 / 6. Lo real fue **52 / 29 / 1 / 4 / 6**, con una
fase que el plan no tenía: la reversión del TRM. El propio plan dice que los
conteos son estimaciones y que *"si una fase cierra menos de lo estimado, es
información, no falla"*, así que esto **no es un defecto** — lo dejo anotado
sólo porque el plan sigue diciendo "cinco fases" y fueron seis pasadas.

## `spec-guardian` — el chequeo que la §5 del Test strategy debía

Lo despaché al agente `atdd:spec-guardian`, de sólo lectura, sobre los 92
escenarios y las 612 líneas de paso. **No se editó ningún archivo**, y no lo voy
a editar: limpiar un spec es trabajo de spec-writer y necesita permiso del dueño
(`CLAUDE.md`).

### El veredicto en una línea

**Cero fugas de implementación en los pasos.** Ni un nombre de clase, tabla,
endpoint, verbo HTTP, framework ni estado interno en las 612 líneas. El
vocabulario acuñado por la feature —`asks`, `holds`, `funded from its
obligations`, `on track`, `behind`— es consistente de punta a punta y es lo mejor
que tiene el documento.

Lo que sí hay está en **cuatro títulos** y en la prosa del preámbulo:

| línea | título | por qué es fuga |
|---|---|---|
| 598 | `No month is stored as a snapshot` | *stored*, *snapshot* son palabras de persistencia |
| 686 | `The fund never reads an account balance` | *reads* es una operación, no un observable |
| 566 | `…with no separate recalculation` | habla de **cómo** se computa, y además no lo afirma ningún paso |
| 887 | `When the daily run happens` | un job agendado; y el título dice *the month end* |

### Y un hallazgo bloqueante, que además explica un bug

**La AC-7 es infalsificable como está escrita.** Se titula *"A fund that gets
drained raises its ask to still arrive"* y sus dos escenarios afirman
`1200000.00 COP` — que es **exactamente lo que pide un fondo intacto**
(`7200000 / 6`, seis meses de nov-2026 a abr-2027).

```gherkin
# spec.md:233
Scenario: An emptied fund raises its ask for the months that remain
  ...
  And the fund on "Seguro" already holds 0.00 COP
  Then the fund on "Seguro" asks 1200000.00 COP this month   ← el baseline
```

Una implementación que **ignore por completo** lo que el fondo tiene pasa las
dos. La AC quedaría firmada como "fijada" sin que nada la fije. La única señal
real del par es la línea 251 (`is behind`), que es también **la única
afirmación de `behind` en los 92 escenarios**.

La reparación es mover el reloj para que el recálculo se vea: con `today` en
`2027-02-10` y el fondo en `0.00`, el pedido tiene que ser `2400000.00`
(7.200.000 en los tres meses que quedan) y no `1200000.00`.

Otros catorce hallazgos menores (títulos que prometen más de lo que verifican,
`"Transporte"` usado sin declararse en 298 y 333, la palabra *balance* haciendo
dos trabajos distintos, el caso degenerado de la AC-24 sin regla escrita) están
en el reporte del agente y se los paso al team lead.

### Calibración

Ninguna de las seis fugas preexistentes de las specs de 002, 005, 006 y 007 que
registró el CP3 de la 008 aparece acá: éste es un documento distinto y los
hallazgos son suyos.

## Las ADRs

### ADR-0043 — **aceptada**, con dos enmiendas `proposed` recortadas

Ya estaba en `accepted` cuando llegué (la dejó un intento anterior, junto con la
fila del índice y los `Superseded by:` de la 0005 y la 0006). **La dejo
aceptada**, y no por inercia: verifiqué las tres cosas que se me pidieron.

- **La frontera de borrado duro describe el código.** `delete_fund` en el
  servicio (F0), `DELETE /funds/{id}` y la herramienta MCP `delete_fund` en el
  tier `write-destructive` (F3), la AC-21 exigiendo borrar el fondo antes de
  archivar la categoría, y la ADR-0005 con su `Superseded by:` en la cláusula de
  metas. El fondo no tiene `archived_at`. ✅
- **El costo del fold sigue siendo cierto.** El refinador midió 156 llamadas a
  `due_dates` para un fold de 12 meses con 6 fondos × 12 obligaciones (la mitad
  de las 312 de antes), y 23 ms con obligaciones desde 2018 contra un
  presupuesto de 50 ms. Crece con los años, tal cual dice la ADR. ✅
- **La enmienda del ancla sigue `proposed`** y no la toqué. La línea `Accepted:`
  lo dice en voz alta para que nadie la arrastre.

**Lo que sí agregué:** una segunda enmienda, también `proposed`, que registra que
las reglas sin fecha **no leen lo que el fondo tiene** y que por eso un fondo
`fixed` o `average` **nunca puede reportarse atrasado**. Es el BUG 2 dicho como
frontera en vez de dejado implícito. Extendí la línea `Accepted:` para que las
**dos** enmiendas queden explícitamente fuera de la aceptación.

Escribí un hecho verificado sobre lo construido y **reservé la decisión de
producto al dueño**. No decidí qué debería significar "en camino" para una regla
sin fecha.

### ADR-0044 — **aceptación retenida**, sigue `proposed`

La mitad que se me pidió revisar está **bien**: la sección de la enmienda
retirada se titula *"Withdrawn: the lazy TRM. ADR-0031 stands unamended"*, la
línea `Supersedes:` dice *"0031 stands unamended"*, las Consequences dicen que
el fallo fuerte sigue uniforme, la Confirmation describe la regla de entrada, y
**no sobrevive ninguna afirmación de comportamiento perezoso**: la única
aparición de *"only when a non-COP amount is actually encountered"* está bajo el
rótulo **"What was proposed."**. Cotejada cláusula por cláusula contra
`product-decisions.md` § ADR-038 —el fallo en la entrada, la razón, el costo
aceptado, el vencimiento con `daily-trm-fetch`, la alternativa (A) rechazada— y
**concuerdan**. ✅

**Pero no la acepté, y ésta es la razón.**

La ADR describe su término `uncovered` como *"everything no fund covers: posted
spending in categories with no fund…"*. Lo construido **no es eso**: salta todo
movimiento posteado con `recurring_id` y cuenta la **promesa** de la obligación
en su lugar. Cuando las dos cifras difieren, el titular no es lo que la ADR dice
que es — y falla **hacia arriba**.

Eso choca con dos cosas que la propia ADR firma:

- con la **AC-9 textual** (*"minus spending in categories no fund covers"*), y
- con **su propio segundo decision driver**: *"The money available never counts
  income that has not arrived; an error here is spent before it can be
  corrected."* El BUG 1 es exactamente ese error, en esa dirección, en ese
  número.

Aceptarla habría sido firmar una descripción que el código contradice donde más
caro cuesta. **Un "todavía no" honesto vale más que un sello.** Dejé la razón
escrita dentro del archivo y devolví su fila del índice a `proposed`, para que la
condición de aceptación sea legible sin este handoff.

## Los dos bugs — severidad y unidad de trabajo

No los re-derivé y no los toqué. Los dos son verdes porque **ningún escenario
aprobado los cubre**: es un hueco de contrato, no una falla de implementación.

### BUG 1 — `_uncovered` pierde plata posteada · **severidad: alta**

Mueve el **titular** —la cifra que el dueño mira todos los días para decidir si
puede gastar— y lo mueve **hacia arriba**, que es la dirección irrecuperable:
plata que se gasta porque la app dijo que estaba. Apagar una obligación después
de que su cargo posteó hace desaparecer los $200.000 enteros.

No hay mitigación en el producto: no hay ninguna otra pantalla donde el dueño
pueda notar el faltante, porque el desglose **cuadra** —`ingreso − Σ piden −
sin cubrir = disponible` sigue dando exacto— con un término mal calculado. Un
número que cuadra consigo mismo y miente es peor que uno que no cuadra.

**Dónde se registra:** en la **ADR-0044**, y ya está registrado — es el bloque
que le retiene la aceptación.

**Unidad de trabajo recomendada: `/engineer.fix`.** Es un defecto localizado, de
una función, con dos reproducciones ya escritas por el refinador y confirmadas
por el team lead. Tiene la forma exacta de un fix: reporte → escenario rojo →
corrección → *"¿por qué no lo agarramos?"*. Y esa última pregunta ya tiene
respuesta, lo que hace el ciclo más valioso: la 0044 declaró la frontera del
lado del ingreso y **nunca la declaró del lado del gasto**.

**No es backlog de consolidación** porque no es deuda tolerable: es plata mal
contada hoy. **No es una feature** porque no hay nada nuevo que decidir sobre el
producto más allá de una regla que la ADR ya tomó del otro lado.

**Bloqueo conocido:** arreglarlo pide un escenario nuevo, y un escenario nuevo es
editar un `spec.md` aprobado → **permiso explícito del dueño** (`CLAUDE.md`).

### BUG 2 — `on_track` es siempre `True` para `fixed` y `average` · **severidad: media**

No mueve ninguna cifra. Miente en un **rótulo**: un fondo que pide $200.000 con
$900.000 gastados pinta el badge verde *"En camino"*, y el `n_behind` del reporte
mensual nunca puede contar esas dos reglas. El daño es confianza en un semáforo,
no plata mal contada — y el dueño tiene el gasto real a la vista en la misma
tabla.

La bajo de alta a media por eso, pero **no más abajo**, porque el semáforo existe
justamente para la regla `average`, que es la que el `feature.md` marca con la
*trampa de retroalimentación* (gastar de más sube el promedio, que sube el
presupuesto, que bendice el gasto de más). Es la regla donde un verde falso hace
más daño.

**Dónde se registra:** en la **ADR-0043**, y ya está registrado — la enmienda
`proposed` que agregué.

**Unidad de trabajo recomendada: feature de seguimiento, pequeña.** No es un fix:
no hay comportamiento correcto que restaurar, **nunca se decidió cuál era**. Hace
falta, en este orden:

1. una **decisión de producto** — qué significa "en camino" para una regla sin
   fecha (el predicado `gastado ≤ apertura + pide` ya existe en el código, pero
   elegirlo cambia un rótulo que el dueño lee);
2. un **AC** que hoy no existe — el bullet *Fund health* del `feature.md` nunca
   llegó a `acs.md`;
3. **escenarios** para las dos reglas sin fecha;
4. y de paso, la reparación del bloqueante de la AC-7, que vive en el mismo
   vecindario.

Meterlo como `/engineer.fix` obligaría a inventar el criterio dentro del fix, que
es exactamente cómo se llega a un semáforo que nadie acordó.

**Y hay una tercera cosa que junta las dos:** el hallazgo bloqueante del
spec-guardian (la AC-7 no distingue un fondo vaciado de uno intacto) y el BUG 2
son **el mismo agujero visto desde dos lados** — el contrato nunca fijó que lo
que el fondo tiene cambie lo que pide ni cómo se reporta. Repararlos por separado
es hacer dos veces la misma conversación con el dueño.

## Lo que NO hice, y por qué

- **No corrí ninguna suite.** Ni `./run-acceptance-tests.sh`, ni pytest, ni
  vitest, ni `just lint`, ni cobertura, ni `crap-analyzer`. El barrido de
  mutación (PID 84534) está reescribiendo `domain/rules.py` y `services/funds.py`
  **en este momento**; cualquier número que hubiera sacado mide fuente mutada y
  sería ficción. **Los dos criterios de salida correspondientes están en
  `met: false`**, no en verde optimista.
- **No leí ni toqué esos dos archivos.** Todo lo que afirmo sobre ellos sale de
  los handoffs. `services/funds.py` además está **sin trackear**, así que no
  existe un `git show HEAD:` con el que contrastarlo — dicho para que nadie
  suponga que lo verifiqué contra fuente.
- **No maté el proceso** ni leí su `mutation-003.json`.
- **No toqué nada bajo `backend/`, `frontend/` ni `acceptance/`.**
- **No modifiqué ningún `spec.md`.** Ni el de la 003 ni el de ninguna otra. El
  hallazgo bloqueante de la AC-7 queda **reportado**, sin tocar.
- **No arreglé los dos bugs.** Los dos cambian lo que el dueño ve y los dos
  necesitan un escenario que hoy no existe.
- **No corregí `plan.md`, `feature.md` ni `acs.md`.** Son artefactos firmados de
  checkpoints anteriores; C1 en particular merece corregirse **antes** de que
  alguien corra el runbook, pero es decisión del team lead.
- **No apliqué ninguna migración.** No corrí `just migrate`, `alembic upgrade`,
  `just dev-prod` ni `just dev-real`. Las revisiones `0011` y `0012` siguen sin
  aplicar y los cinco pasos del runbook sin ejecutar — que es lo **correcto**.
- **No hice commit ni push.**
- **No entré en una guerra de ediciones** con la otra sesión. Una corrección,
  registrada, y escalado.

## ¿Se necesita acción humana? Sí

**Del team lead:**

1. **Quién tiene la pluma sobre las ADRs**, mientras haya más de una sesión viva
   en este repo.
2. **La corrección de `plan.md`** — la `0011` / `0012` (C1), antes de que nadie
   corra el runbook.
3. **La unidad de trabajo de cada bug**: `/engineer.fix` para el 1, feature
   pequeña para el 2 (con la AC-7 adentro).
4. **Las cuatro filas de consolidación** que el refinador propuso y no filó.

**Del dueño:**

5. **Las dos enmiendas `proposed` de la ADR-0043** — el mes del ancla, y las
   reglas sin fecha que no leen lo guardado.
6. **Qué significa "en camino"** para un fondo `fixed` o `average`.
7. **Permiso para editar el `spec.md` aprobado** — lo necesitan la reparación de
   la AC-7, el escenario del BUG 1 y los escenarios del BUG 2. Sin ese permiso
   los tres quedan bloqueados.

## Siguiente paso recomendado

Cuando el barrido termine: leer `backend/.build/mutation-003.json`, correr
cobertura y `crap-analyzer` sobre un árbol limpio, y cerrar los dos criterios que
dejo en `met: false`. **Eso es CP8 y no es de este agente** — pero tampoco es de
`refiner-1` ni de ninguno de los cinco implementadores (Principio 7).
