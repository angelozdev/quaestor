---
skill: verify
agent_id: architect-cp7-cp8
feature: 010-self-explaining-screens
started: 2026-08-08T0045
ended: 2026-08-08T0245
checkpoint: 7
artifacts:
  - features/010-self-explaining-screens/handoffs/2026-08-08T0245-verify.md
exit_criteria:
  - criterion: The five gates hold at the CP6 baseline, every one re-run by the verifier
    verified_by: tool
    met: true
    evidence: "Re-run by me on a tree I confirmed clean first (`git status --short` -> empty). `./run-acceptance-tests.sh` -> '361 passed, 1 warning in 15.58s', EXIT=0. `cd backend && SESSION_SECRET=… uv run pytest -q` -> '992 passed, 1 warning in 33.60s'. `cd frontend && pnpm vitest run` -> 'Test Files 54 passed (54) / Tests 361 passed (361)'. `pnpm tsc --noEmit` -> exit 0, no output. `just lint` -> exit 0, 'Checked 186 files in 22ms. No fixes applied. / Found 4 warnings. / Found 1 info.' — the same 4+1 pre-existing warnings 003 recorded. `python3 acceptance/spec_coverage.py features/010-self-explaining-screens frontend` -> 'scenarios 115 / bound to tests 100 / @browser 2 / @backend 13 / unbound 0'. `dae_handoff.py features/010-self-explaining-screens` -> 'ok -- latest complete checkpoint: 6'. Every figure identical to the CP6 handoff's."
  - criterion: Counts agree across spec.md, the IR, the generated tests and the ACs
    verified_by: tool
    met: true
    evidence: "Four independent paths. 115 `Scenario:` headings in `spec.md` = 115 scenarios in `.build/spec.json` = 115 reported by `spec_coverage.py`. Per-AC scenario counts sum to exactly 115 over 21 AC headings, and `acs.md` carries `ac_count: 21` against 21 AC headings — every AC has at least one scenario. The 13 `@backend` names read from `spec.md` map one-for-one onto the 13 `def test_` functions in `.build/generated/test_010_self_explaining_screens.py` (checked by re-deriving each name through `generator._sanitize`: 'missing from generated: []', 'generated but not tagged: []'). `spec.md`'s preamble arithmetic also checks out: AC-18 has 7 scenarios and all are `@backend`, AC-13 contributes 1 and AC-3 contributes 5, which is the 13."
  - criterion: The artifacts agree with each other and with the code
    verified_by: judgment
    met: false
    evidence: "THREE STALE STATEMENTS, all describing the feature as it was before CP3 took it out of frontend-only. (1) MATERIAL — `acs.md:78`: 'Not one line of the arithmetic moves. `domain/rules.py` and **`services/funds.py`** are untouched, no migration, no schema change.' `services/funds.py` gained `_look_ahead`, `_opening_next`, `_Month.carries` and three `FundStatus` fields on this branch. `feature.md` and `spec.md` both narrate the change explicitly; `acs.md` carries no note at all, so a reader of `acs.md` alone is told the opposite of what shipped. The AC-18 *intent* is intact — `domain/rules.py` really is untouched and no existing figure moved — it is the file list that is wrong. (2) `feature.md:154-155`: 'the generated stream for the **nine** `@backend` scenarios, and the Chrome MCP for the **one** scenario about layout'. Actual: 13 and 2. (3) `plan.md:10` and `plan.md:129` say '113 scenarios', `plan.md:231` says 'the **98** untagged scenarios'. Actual: 115 and 100 — `spec.md` grew by AC-9's two outside-press scenarios at CP4, with the owner's permission. What I checked and found NOT stale: `plan.md:250`'s baseline '348 acceptance, 253 vitest' is correct for the pre-feature tree (44 frontend test files at the merge-base 99a840d, 253 tests; 348 + 13 generated = today's 361), and `feature.md:103`'s '`domain/rules.py` untouched' is true. Recorded as `met: false` because it is a real disagreement between a signed artifact and the code, and correcting a signed artifact is the team lead's call, not mine."
  - criterion: Coverage and CRAP measured over the changed backend surface
    verified_by: tool
    met: true
    evidence: "Coverage generated over BOTH streams into one dataset, 003's lesson: `uv run --with pytest-cov pytest -q --cov=src/quaestor --cov-branch tests` (992 passed) then `--cov-append` over `../features/*/.build/generated` (361 passed). By changed module: `services/funds.py` 98% (295 stmts, 4 miss, 84 branch, 2 partial), `domain/dtos.py` 100%, `api/schemas.py` 100%, `mcp/format.py` 94%, and `domain/rules.py` 97% untouched. `compute_crap.py --diff <git diff 99a840d..HEAD --relative=backend -- backend/src> --threshold 0` -> 5 findings, ZERO above the threshold of 20: `_walk` CRAP 4.0 (complexity 4, 100%), `_status` 4.0 (4, 100%), `mcp/format.fund_card` 2.06 (2, 75%), `_opening_next` **2.0 (complexity 2, coverage 100%)**, `_look_ahead` **2.0 (complexity 2, coverage 100%)**. The two functions this branch invented are the two lowest-risk things in its own diff. Frontend: `pnpm vitest run --coverage` over the changed modules -> `screen-help.tsx` 100% stmts / 92.85% branch (uncovered 26, 92 — both defensive guards), `empty-state.tsx` 100% / 92.85% (44, the icon ternary), `page-header.tsx` 100/100, `lib/funds.ts` 100/100, `lib/date.ts` 100/100."
  - criterion: "`_look_ahead` costs no query — re-measured, not taken from CP6"
    verified_by: tool
    met: true
    evidence: "A/B with the repo's own `tests/support/query_counter.count_queries`, on a probe I wrote, ran and then DELETED (`git status --short` clean afterwards). Seeded 5 funds over 80 months of history each — one per rule INCLUDING `from_recurring` with a yearly obligation, which is the rule plan §A named as the risk and which CP6's 4-fund measurement did not include. Result: `PROBE look_ahead: with=11 without=11 funds=5 next_month_has=[721000000, 1000000, 39247701, 5000000, 117150615] stubbed=[0, 0, 0, 0, 0]`. **11 SQL statements either way.** The stub is proven to have disabled the work by the third figure: every `next_month_has` is 0 with `_look_ahead` monkeypatched and non-zero without. CP6's claim holds and now holds over a wider setup. ADR-0028's bounded read path is untouched: `BOUNDED_LOADS` is still 10 and `test_load_issues_bounded_query_count` still passes inside the 992."
  - criterion: Bindings are real, proven by breaking the implementation
    verified_by: tool
    met: true
    evidence: "The coverage gate matches on NAME ONLY, so I broke seven implementation sites and checked that the tests carrying the scenario names failed. (1) `app-shell.tsx` nav label -> 'Fondos': `The navigation names both shapes` FAILED, 1/1. (2) `lib/funds.ts` `shapeOf` inverted: 24 tests over 3 files FAILED, including `A presupuesto's own row never calls it a fondo`, `The Dashboard breakdown calls a presupuesto a presupuesto`, `The Reportes breakdown calls a presupuesto a presupuesto`, `A pre-split fund that carries money forward reads as a fondo` and all six AC-6 filing scenarios. (3) `screen-help.tsx` focus return removed from `close`: 3 FAILED — `Escape closes the panel and gives the reader back their place`, `Clicking outside the panel closes it`, and `gives the reader their place back however the panel is closed`. (4) `empty-state.tsx` stops rendering `description`: 12 FAILED, 9 of them AC-10's named scenarios across nine screens. (5) `funds/page.tsx` quotes `carries` where it should quote `next_month_has`: 2 FAILED, exactly `A fondo's row states what next month will have to spend` and `A presupuesto's row states that the leftover money is lost, with its figure`. (6) `_look_ahead` drops next month's ask: 4 of the 13 generated `@backend` tests FAILED with the spec path and the scenario name in the message. (7) `_status` reports `spent=0`: `A fund reports what the category spent this month` FAILED, alone and correctly. Every source file was restored with `git checkout --` and the tree re-verified clean. SEPARATELY, a static audit over all 115 scenarios: each name resolves to exactly one `it()` in exactly one file, none is `it.skip`/`it.todo`, and every block contains at least one `expect(` — 'weak bindings: 0'."
  - criterion: The two @browser scenarios' recorded evidence is judged on its own merits
    verified_by: judgment
    met: false
    evidence: "I did not re-run them — no live stack, and they are the team lead's. Judged as written. AC-14 (`The panel wraps instead of running off a phone screen`): THE METHOD IS SOUND — a 390px same-origin iframe is a real viewport with the browser's own layout engine and media queries, and the handoff proves it was one (`iframeInnerWidth 390`, `matchMedia('(max-width: 640px)').matches true`) before measuring anything. All THREE Thens were measured with the right quantity: 'no text clipped' -> 0 leaf nodes whose scrollWidth exceeds clientWidth; 'nothing past 390px' -> 0 descendants; 'no sideways scroll' -> documentElement.scrollWidth 375 vs innerWidth 390. The panel's 375 against the iframe's 390 is the 15px scrollbar the same handoff measures elsewhere, so the numbers are internally consistent. THE GAP: **the Given was substituted.** The scenario names three specific funds — `Mantenimiento del carro` (a 22-character label, the longest string in the setup and precisely the thing a wrapping test exists to stress), `Restaurantes`, `Servicios` — and the sandbox held Transporte, Mercado and Restaurantes instead. The evidence therefore shows that the panel wraps for *some* data, not for *the* data the scenario specifies. Low risk, but low risk is not measurement. AC-7 (`The control sits in the same place on every screen`): the Given IS complete — all ten screens, in turn. 'It looks the same' is met outright and strongly: 167x30 at 12px on 10/10. 'It is in the same position' is NOT literally met: the x takes two values, 1237 and 1244. The explanation offered — right=1237 on exactly the four screens reporting `scrollbar: 15` and 1244 on the six reporting `scrollbar: 0` — is a mechanism, correlates perfectly across ten samples, and I believe it. But it is an interpretation of the Then rather than the Then, and it was avoidable: measuring the trigger's offset inside its own header, or setting `scrollbar-gutter: stable`, would have produced one number. Recorded as `met: false` so the substitution and the interpretation are on the record; NEITHER is a reason to hold the feature."
  - criterion: The known-open items are what the CP6 handoff says they are
    verified_by: tool
    met: true
    evidence: "All four confirmed, none re-litigated. (1) `spec_coverage.test_corpus` walks the whole `frontend` tree before filtering: measured 'rglob walked 88894 paths, read 54 files (169979 bytes) in 2097 ms' — CP6 recorded 4011 ms; the difference is the OS file cache, the defect is the same. (2) `run-acceptance-tests.sh` carries the three prose comment lines this branch added at 50-52 (plus a `shellcheck disable` pragma at 83, which is a tool directive rather than prose). (3) `acceptance/handlers/sinking_funds.py:253-260` is a triple-quoted string floating after two assignments — a comment in a docstring's clothes. (4) `exclude_from_budget` is dead across the whole stack: written by `services/categories.py` (whose own docstring says 'Stored and read by nothing since feature 003 took the envelope out'), carried by `domain/models.py`, `api/schemas.py`, `api/routers/categories.py`, `mcp/tools/masters.py` and `frontend/lib/api/types.ts`, and read by nothing anywhere. (5) `screen-help.tsx:123` declares `aria-modal=\"true\"` while only the backdrop div carries `aria-hidden`; nothing behind the panel is `inert` or hidden, so a screen reader can still walk the page underneath. Confirmed by reading the component. ALSO CONFIRMED, from CP6's tracker note: four `FundStatus` fields are read by no frontend code — `accumulation_is_implied`, `averaged_over`, `spreads_over`, `whole_by` (the last two do reach the MCP card, so 'read by nothing' is frontend-only). All three fields THIS branch added are read."
  - criterion: The branch created no dead code
    verified_by: tool
    met: true
    evidence: "Every new export has a production consumer, not just a test: `shapeOf` (app/(app)/page.tsx, funds/page.tsx), `accumulatesAs` (funds/create-form.tsx), `nounOf`/`whatItIs`/`shapeSentence` (funds/page.tsx), `labelOf` (page.tsx, reports/page.tsx), `nextYearMonth`/`monthNameOf` (funds/page.tsx), `ScreenHelp` (11 files), `HelpSection`/`HelpExample` (3), `HELP_LABEL` (5). On the backend, mutation independently confirms `_look_ahead`'s and `_opening_next`'s guards are live rather than defensive — see the CP8 handoff. CP6's claim holds."
  - criterion: "No `spec.md`, generated test, migration or source file was changed by verification"
    verified_by: tool
    met: true
    evidence: "`git status --short` at the close of CP7 shows nothing but `?? backend/.coverage`, a regenerable artifact I produced and then removed. Seven source files were broken and restored with `git checkout --`, each verified clean immediately after. The query probe lived at `backend/tests/test_zz_verify_probe.py` for one run and was deleted. No file under `features/*/spec.md`, `.build/generated/` or `backend/src/quaestor/migrations/` was touched, and `just migrate`, `alembic upgrade`, `just dev-prod` and `just dev-real` were not run. `.dev-data/` was not written. The five tests added under CP8 are test-only and belong to the harden handoff."
findings_summary: "CP7 signs as a measurement. The five gates hold at the CP6 baseline exactly — acceptance 361/exit 0, backend 992, vitest 54/361, tsc 0, lint 0 — and the counts agree four ways. CRAP: zero of the five changed backend functions above the threshold of 20; `_look_ahead` and `_opening_next`, the branch's new arithmetic, both score 2.0 at 100% coverage. THE LOAD-BEARING CP6 CLAIM SURVIVES RE-MEASUREMENT AND WIDENS: `_look_ahead` costs 11 SQL statements against 11 with it stubbed out, measured over FIVE funds including the `from_recurring` rule the plan named as the risk and that CP6's four did not exercise. THE BINDINGS ARE REAL: the coverage gate matches on name alone, so I broke seven implementation sites and the tests carrying the scenario names failed every time — 47 distinct named scenarios across the two streams, including all six AC-6 filing scenarios and nine of AC-10's ten screens; a static pass over all 115 found no skipped test and no assertion-free block. TWO EXIT CRITERIA ARE FALSE AND BOTH ARE JUDGEMENTS, NOT UNFINISHED WORK. First, three artifact statements still describe the pre-CP3 frontend-only feature, and one of them is material: `acs.md` states that `services/funds.py` is untouched, which is the file this branch changed; `feature.md` says nine `@backend` and one `@browser` where there are 13 and 2; `plan.md` says 113 scenarios and 98 untagged where there are 115 and 100. Second, the `@browser` evidence is good but not airtight: AC-14's method is sound and all three Thens were measured with the right quantities, but the Given was substituted — the scenario's three named funds, including the long `Mantenimiento del carro` label a wrapping test exists to stress, were not the ones on screen; AC-7's 'same position' resolves to two x values explained by scrollbar width rather than to one number. TWO THINGS NOBODY HAD LOOKED AT. **The MCP/REST parity the plan's Charter Check claims is honoured is real in the code and untested anywhere**: deleting all three new lines from `mcp/format.fund_card` leaves 992 backend and 361 acceptance green. And `./run-acceptance-tests.sh` exiting 0 on a `mixed` feature proves that a test with each scenario's NAME exists, never that vitest passes — the runner never invokes vitest, so the acceptance gate and the frontend gate have to be run as two commands or the green is half a green."
human_action_needed: yes
human_action_kind: decision
recommended_next: "Team lead, on the three stale statements: `acs.md:78` is the one that matters and `/engineer.feature-edit` is the route — I did not touch a signed artifact. Owner, on nothing blocking. For the roadmap, three rows from CP7 on top of CP6's: the MCP fund card's three new lines have no test; `run-acceptance-tests.sh` does not run vitest and its exit 0 is therefore not the frontend gate; and the AC-7 `@browser` scenario would become a one-number check with `scrollbar-gutter: stable`. CP8 follows in this same session, same agent_id, and its handoff carries the mutation sweep."
tracker_update: "local — 010 advances to checkpoint 7 complete. Gates identical to the CP6 baseline. Two exit criteria `met: false` by judgment, both documentation/evidence items rather than code defects; nothing found that blocks the feature."
status: complete
---

# verify — resumen del handoff

## Sobre la autoría

Mío y de primera mano. No escribí una línea de esta feature. Leí los ocho
handoffs como **afirmaciones que había que probar**, no como reportes que había
que resumir, y las tres que sostienen el trabajo las volví a medir yo.

Mi `agent_id` es `architect-cp7-cp8` y difiere de los cinco anteriores
(`main-session`, `implementer-phase12`, `implementer-phase3`,
`implementer-phase4`, `main-session-refiner`), que es lo que exige el
Principio 7 y lo que `dae_handoff.py` verifica.

## Las cinco compuertas

Corridas por mí sobre un árbol que **verifiqué limpio antes de medir**.

| | línea base CP6 | ahora |
|---|---|---|
| `./run-acceptance-tests.sh` | 361 / exit 0 | **361 passed**, EXIT=0 |
| Backend `uv run pytest -q` | 992 | **992 passed** |
| Frontend `pnpm vitest run` | 54 / 361 | **54 passed (54) / 361 passed (361)** |
| `pnpm tsc --noEmit` | 0 | **exit 0** |
| `just lint` | 0 | **exit 0**, 4 warnings + 1 info |
| `spec_coverage.py` | 115 / 100 / 2 / 13 / 0 | **idéntico** |

Los conteos cuadran por cuatro caminos independientes: 115 `Scenario:` en el
`spec.md`, 115 en el IR, 115 que reporta la compuerta, y la suma por AC sobre
21 encabezados. Los 13 nombres `@backend` del `spec.md` caen uno a uno sobre
las 13 funciones generadas.

## Lo que sí volví a medir, y no solo a leer

### `_look_ahead` no cuesta una consulta — y ahora sobre un caso más duro

El CP6 lo midió con 4 fondos. **Yo lo medí con 5, incluyendo la regla
`from_recurring`**, que es justo la que el plan §A nombró como el riesgo de
sacar esta feature de *frontend-only* y la que los 4 del CP6 no ejercitaban.

```
PROBE look_ahead: with=11 without=11 funds=5
  next_month_has=[721000000, 1000000, 39247701, 5000000, 117150615]
  stubbed=[0, 0, 0, 0, 0]
```

**11 sentencias SQL con y sin.** La tercera línea es la que hace que la
medición valga: con el `_look_ahead` anulado **todas** las cifras son 0, o sea
que el sustituto de verdad apagó el trabajo. La sonda vivió un solo run en
`backend/tests/` y la borré.

### Las ataduras son reales — siete roturas, cuarenta y siete escenarios

La compuerta `spec_coverage.py` **compara por nombre y nada más**. Un test
bautizado como un escenario que afirme otra cosa pasa todas las compuertas de
este repo. Así que rompí el código y miré si caía el test que lleva el nombre.

| lo que rompí | qué cayó |
|---|---|
| la etiqueta del menú | `The navigation names both shapes` |
| `shapeOf` invertido | **24 tests en 3 archivos** — las seis de AC-6, las dos de AC-13, las dos de los desgloses |
| el retorno de foco de `close` | 3 juntas: `Escape…`, `Clicking outside…`, y la del componente |
| `EmptyState` deja de mostrar `description` | **12**, nueve de las diez pantallas de AC-10 |
| la fila cita `carries` en vez de `next_month_has` | exactamente las 2 de AC-3 que hablan del mes siguiente |
| `_look_ahead` pierde el `ask` | 4 de los 13 generados `@backend` |
| `spent` reportado como 0 | `A fund reports what the category spent this month`, sola |

Cada archivo quedó restaurado con `git checkout --` y el árbol re-verificado.

Además, una pasada estática sobre los 115: cada nombre resuelve a **un solo**
`it()` en **un solo** archivo, ninguno es `it.skip` ni `it.todo`, y ninguno
tiene cero `expect(`. **Ataduras vacías: 0.**

## Los dos escenarios `@browser`, juzgados sin saber el resultado

No los volví a correr —no tengo la pila viva y son del líder del equipo—. Los
juzgué como están escritos.

**El AC-14 tiene el método bien.** Un iframe de 390 px del mismo origen *es* un
viewport: motor de layout, CSS y media queries son los del navegador. Y el
handoff lo demuestra antes de medir (`iframeInnerWidth 390`,
`matchMedia('(max-width: 640px)').matches true`). Los **tres** *Then* se
midieron con la cantidad correcta, y los números se sostienen entre sí: el
panel de 375 dentro de 390 es la barra de 15 px que el mismo handoff mide en la
otra verificación.

**Lo que falta es el *Given*.** El escenario nombra tres fondos concretos —
`Mantenimiento del carro`, que es la etiqueta más larga del montaje y
exactamente lo que una prueba de wrapping existe para estresar— y el sandbox
tenía otros tres. La evidencia muestra que el panel envuelve **con unos datos**,
no **con los datos que el escenario pide**. El riesgo es bajo; bajo no es
medido.

**El AC-7 tiene el *Given* completo** —las diez pantallas, una por una— y
*"looks the same"* queda probado de sobra: 167x30 a 12 px en 10 de 10. *"In the
same position"* **no se cumple al pie de la letra**: hay dos valores de x, 1237
y 1244. La explicación —1237 en exactamente las cuatro pantallas que reportan
`scrollbar: 15`— es un mecanismo, correlaciona perfecto en diez muestras y me la
creo. Pero es una **interpretación** del *Then*, no el *Then*; y era evitable
midiendo el `offsetLeft` dentro de su propio header.

**Ninguna de las dos cosas justifica frenar la feature.** Las dos quedan
escritas porque el ADR-0045 dice que la evidencia registrada *es* el verde.

## Las tres frases rancias

La feature cambió de forma dos veces y la prosa no la siguió entera.

1. **La que importa — `acs.md:78`:** *"`domain/rules.py` y **`services/funds.py`**
   están sin tocar"*. `services/funds.py` es **el archivo que esta rama cambió**.
   El `feature.md` y el `spec.md` narran el cambio de alcance en párrafos
   propios; el `acs.md` no dice nada, así que quien lea solo el `acs.md` recibe
   lo contrario de lo que se construyó. **La intención de la AC-18 sigue
   intacta** —`domain/rules.py` de verdad está sin tocar y ninguna cifra
   existente se movió—; lo que está mal es la lista de archivos.
2. **`feature.md:154-155`:** *"nueve escenarios `@backend`"* y *"el único
   escenario sobre layout"*. Son **13** y **2**.
3. **`plan.md:10`, `:129` y `:231`:** *"113 escenarios"* y *"los 98 sin
   etiqueta"*. Son **115** y **100**, desde que el `spec.md` creció con los dos
   escenarios del cierre por fuera del AC-9 en el CP4.

**Lo que revisé y NO está rancio**, para que no se corrija de más: la línea base
del `plan.md:250` (*348 acceptance, 253 vitest*) es correcta para el árbol
anterior a la feature —44 archivos de test de frontend en el `merge-base`
`99a840d`, y 348 + 13 generados = los 361 de hoy—; y `feature.md:103`
(*`domain/rules.py` untouched*) es verdad.

**No toqué ninguno.** Corregir un artefacto firmado es del líder del equipo.

## Dos cosas que nadie había mirado

### La paridad MCP/REST está en el código y no la prueba nadie

El *Charter Check* del plan marca la §2 con ⚠️ y dice que la paridad *"se está
honrando, no doblando"*: la tarjeta MCP del fondo gana los tres campos en la
misma fase que REST. **Es cierto en el código.** Lo que no hay es una prueba.

Borré las **tres** líneas nuevas de `mcp/format.fund_card` y corrí todo:

```
992 passed        (backend)
361 passed        (aceptación, exit 0)
```

Verde entero. `tests/mcp/test_funds.py` afirma `Asks:` y `Holds:` y nada más.
La cobertura de línea no lo ve porque las tres líneas viven dentro de un mismo
literal de lista que sí se ejecuta. **Una regla del charter que sólo el código
sostiene se rompe la primera vez que alguien reordena esa lista.**

### `./run-acceptance-tests.sh` en exit 0 **no** es la compuerta del frontend

Para una feature `frontend` o `mixed` el runner corre `spec_coverage.py`
—que sólo comprueba que exista un test **con ese nombre**— y genera el
subconjunto `@backend`. **Nunca invoca `pnpm vitest`.** Su código de salida
combina `PYTEST_STATUS` y `FRONTEND_FAILED`, y ninguno de los dos puede ponerse
en rojo por un test de vitest que falle.

O sea: `./run-acceptance-tests.sh` puede salir 0 con la suite de frontend en
rojo. Aquí no pasa —corrí `pnpm vitest run` aparte y da 54/361— pero la
compuerta son **dos comandos**, y quien corra sólo el primero se lleva medio
verde creyendo que es entero.

## Lo que confirmé sin re-litigar

Los cinco puntos abiertos del CP6 están **exactamente** como los dejó:

- `spec_coverage.py` recorre **88.894 rutas para leer 54 archivos** (medido
  2.097 ms hoy contra 4.011 ms en el CP6 — la diferencia es la caché del
  sistema de archivos, el defecto es el mismo);
- las tres líneas de comentario en `run-acceptance-tests.sh:50-52` siguen ahí
  (más un `shellcheck disable` en la 83, que es directiva de herramienta);
- la cadena flotante en `acceptance/handlers/sinking_funds.py:253-260` sigue
  ahí;
- el panel declara `aria-modal="true"` y **sólo el backdrop** lleva
  `aria-hidden`; nada detrás está `inert`;
- `exclude_from_budget` sigue muerto en toda la pila —lo escribe
  `services/categories.py`, cuyo propio docstring lo dice, y no lo lee nadie.

Y de la nota del tracker del CP6: los **cuatro** campos de `FundStatus` que no
lee el frontend son `accumulation_is_implied`, `averaged_over`, `spreads_over`
y `whole_by` (los dos últimos sí llegan a la tarjeta MCP, así que *"no los lee
nadie"* es sólo del frontend). **Los tres campos que agregó esta rama sí se
leen.**

## Lo que NO hice

- **No toqué ningún `spec.md`.** Ni el de la 010 ni ningún otro.
- **No edité nada bajo `.build/generated/`.** Todos los conteos salen del
  runner, que regenera desde el IR.
- **No corregí `acs.md`, `feature.md` ni `plan.md`.** Son artefactos firmados.
- **No cambié comportamiento.** Las siete roturas se revirtieron con
  `git checkout --` y el árbol quedó verificado limpio después de cada una.
- **No toqué `backend/src/quaestor/migrations/**`**, no corrí `just migrate`,
  `alembic upgrade`, `just dev-prod` ni `just dev-real`, y no escribí en
  `.dev-data/`.
- **No corrí los dos `@browser`.** Los juzgué; son del líder del equipo.
- **No hice commit ni push.**
