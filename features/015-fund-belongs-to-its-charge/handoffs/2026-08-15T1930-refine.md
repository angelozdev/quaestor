---
skill: refine
agent_id: main-session + three fresh review subagents
feature: 015-fund-belongs-to-its-charge
started: 2026-08-15T1840
ended: 2026-08-15T1930
checkpoint: 6
artifacts:
  - backend/src/quaestor/services/funds.py
  - backend/src/quaestor/services/transactions.py
  - backend/src/quaestor/api/schemas.py
  - backend/src/quaestor/api/routers/transactions.py
  - backend/src/quaestor/domain/report_markdown.py
  - backend/tests/services/test_transactions.py
  - frontend/app/(app)/recurring/page.tsx
  - frontend/app/(app)/reports/page.tsx
  - frontend/app/(app)/reports/page.test.tsx
  - frontend/components/settles-charge-field.tsx
  - frontend/components/transaction-create-dialog.tsx
  - frontend/components/transaction-create-dialog.schema.ts
  - frontend/components/transaction-create-dialog.test.tsx
  - frontend/components/transaction-edit-dialog.test.tsx
  - frontend/lib/api/types.ts
findings_summary: "THREE REVIEW SUBAGENTS OVER THE BRANCH'S 40 CHANGED SOURCE FILES FOUND THREE DEFECTS THAT THREE GREEN SUITES DID NOT. (1) AC-5 WAS GREEN AGAINST THE WRONG SCREEN. The spec says `When the owner records an expense in \"Carro\"`, and both bound vitest tests rendered the EDIT dialog for an already-saved movement; `transaction-create-dialog.tsx` had `MetaField` but no `SettlesChargeField`, so the owner typing a new expense was offered nothing and had to save it and reopen it. The owner was asked and chose the offer belongs on the saving screen, as the AC is written. Fixed: `TransactionCreate` carries `recurring_id` end to end, the field mounts beside the meta field, the two scenario-named tests moved to the create-dialog suite, and the edit-dialog pair was renamed so one scenario name binds to one screen. Two more tests were added there: the chosen charge reaches the request, and money coming in is never asked which charge it settled. (2) THE MONTHLY REPORT SAID PESOS WHERE THERE WERE DOLLARS. The branch made `FundReportLine` currency-aware and then did not propagate it to four consumers — `report_markdown.py` (`money(f.asks)` with no currency, one row above the metas doing it correctly), `FundReportLineOut`, the TS interface, and the three Reportes columns. A fund on a USD charge rendered `$50,00` where the truth was 157.100 pesos. AC-11 names this defect class by name; every currency scenario asserts through `fund_status` and none through the report, which is why it went green. (3) THE RECURRING SCREEN COMPUTED ITS MONTH IN UTC while the other five screens use local `format(new Date(), \"yyyy-MM\")` — from 19:00 on the last day of the month in Bogotá it marked charges into a month no other screen was looking at. ONE DOCSTRING THAT WOULD HAVE DELETED FUNDS, found independently by two lenses: `would_lose_its_fund` claimed to measure with `_why_not_markable` while the code deliberately measures with `_can_still_keep_a_fund`, because `_why_not_markable` also refuses a charge landing this very month — the month a fund exists for. The prose instructed the next reader to make exactly the substitution its sibling docstring warns kills funds. TWO WASTE FIXES, EACH FLAGGED BY ALL THREE LENSES: `settles-charge-field.tsx` keyed a query on a `categoryId` its own `queryFn` ignored and then wrapped a SECOND query around data it already held, whose key did not change when that data refetched — so it served a stale option list; it now narrows inside the query, one key, one cache entry, spread into `EntitySelect` the way `MetaField` does. And every tick of Juntar fired two identical GETs, because `invalidate(qc, \"recurringWrite\")` already prefix-matches `qk.chargeMarks(month)` and the `void marks.refetch()` beside it cancelled that request to start another. `record_income` now accepts `recurring_id` so `refuse_bad_charge_link` refuses rather than the link being dropped in silence, pinned by a new test. WHAT CAME BACK CLEAN IS ALSO EVIDENCE: jscpd reported 7 duplicate clusters inside the branch's files and ALL SEVEN ARE INHERITED — traced to `2722fd9`, none introduced; the 498 step patterns across 11 handler modules have zero collisions; the layering contracts hold; `_settled_by_spending` is NOT dead (still reached by the category-wide `from-recurring` rule, which `create_fund` still offers — flagged as a product question, not a refactor); and the helpers the brief suspected of being reinvented (`months_to_fund`, `fund_ask_calc`, `to_cop_cents`, `_turn_after`) all predate the branch and are reused. THE BOUNDED READ PATH WAS VERIFIED RATHER THAN TRUSTED: measured with the query counter at 6, 6, 20+20+40 and 40+40+80 funds, `load_month_aggregate` issues 13 statements flat and `month.available()` 14 — the feature added ZERO queries. Ten smaller findings were charter-filtered or judged not worth the churn and are listed in the body for CP8."
human_action_needed: no
recommended_next: "crap-analyzer for Light Verify (CP7), then the CP8 mutation sweep — on a freshly migrated worktree, since test_scheduler touches backend/quaestor.db on disk."
tracker_update: "none — roadmap item already in-progress."
exit_criteria:
  - criterion: "the changed code was reviewed by agents independent of the implementer"
    verified_by: tool
    met: true
    evidence: "Three fresh review subagents (Reuse, Quality, Efficiency), each dispatched over `git diff 2722fd9..HEAD` with the same three hard constraints: no code comments (CLAUDE.md), the ACs+spec contract is sacred, measure before optimising. Independence is satisfied by construction — none of them wrote the code."
  - criterion: "a deterministic duplication scan ran and its findings were judged"
    verified_by: tool
    met: true
    evidence: "dae_dup.py (jscpd, 50 tokens / 5 lines) → status ok, 26 clusters project-wide, 7 inside or beside this branch's files. Each of the 7 was traced against `2722fd9` and `git log -L`: all inherited, none introduced by the branch. `to-pay/page.tsx` is not modified by the branch at all."
  - criterion: "every proposal applied keeps the behaviour contract"
    verified_by: tool
    met: true
    evidence: "The contract is not merely kept — three of the fixes are the contract being met where it was not. AC-5's offer now appears on the screen the scenario names; the report renders each fund in its own currency, which is what AC-11 requires; the Recurring screen marks into the month the rest of the app is in. Nothing else changed observable behaviour."
  - criterion: "both test streams are green after applying"
    verified_by: tool
    met: true
    evidence: "backend: 1214 passed, 1 warning in 63.57s (+1, the income-cannot-settle refusal). acceptance: ./run-acceptance-tests.sh → 711 passed. frontend: 58 files, 573 tests passed (+4, the create-dialog AC-5 set). spec-coverage for 015: 50 scenarios, 39 @backend, 11 bound, 0 unbound — and the two AC-5 scenario names now resolve to the create dialog, one name to one screen."
  - criterion: "lint, formatting, types and architecture contracts pass"
    verified_by: tool
    met: true
    evidence: "just lint → exit 0 (ruff check: All checks passed; ruff format: 238 files already formatted; biome: 204 files, 4 pre-existing warnings, no errors). cd frontend && pnpm tsc --noEmit → exit 0. cd backend && uv run lint-imports → Contracts: 2 kept, 0 broken."
  - criterion: "no charter-violating proposal was applied"
    verified_by: inspection
    met: true
    evidence: "Two were filtered out. Retiring the category-wide `from-recurring` rule (which would make `_settled_by_spending` dead) changes observable behaviour and appears in no AC — recorded as a product question. Suppressing the Juntar column's reason on income rows is likewise a product call. No proposal added a code comment; the two docstrings edited are the allowed convention."
status: complete
---

# CP6 — Refine

Tres lentes sobre los 40 archivos que cambió la rama. **Un solo commit**,
`c21cf4f`, porque los seis arreglos son la misma historia: cosas que tres
suites verdes no podían ver.

## La puerta de entrada bloqueó, y eso fue correcto

`dae_handoff.py --through 5` salió en 1: el CP5 nunca tuvo handoff. Lo escribí
antes de seguir, pero **volviendo a medir las nueve cifras** en vez de citarlas
de memoria — un criterio dado por cumplido sobre prosa es exactamente lo que
este proyecto ya pagó una vez.

## Lo que se arregló

| | qué decía | qué era |
|---|---|---|
| AC-5 | verde | verde contra la pantalla de editar, no la de guardar |
| Informe | `$ 50,00` | `US$ 50,00` — 157.100 pesos de diferencia |
| Recurrentes | agosto | septiembre, el día 31 después de las 7 pm |

Y una línea de prosa que le pedía al próximo lector la sustitución que borra
una caja el mes en que llega su cobro.

## Lo que salió limpio, que también es evidencia

```
jscpd            7 grupos en la rama  →  los 7 heredados, 0 introducidos
handlers         498 patrones          →  0 colisiones
capas            2 contratos           →  0 rotos
lectura del mes  6 · 20+20+40 · 40+40+80 fondos  →  13 consultas, plano
```

El camino de lectura **se midió**, no se creyó. La 015 agregó cero consultas.

## Lo que dejo para el CP8

Diez hallazgos menores, ninguno visible para el dueño: el diccionario anidado
que guarda una sola llave (`month_aggregate`), tres guardas que su único
llamador no puede activar (`unmark_if_it_can_no_longer_be_saved_for`),
`_charge_obligation` diciendo lo mismo dos veces, `_fills_for` devolviendo
`None` por dos razones distintas que sus lectores tratan distinto, dos
ayudantes copiados entre módulos de handlers, y `obligation()` recorriendo una
lista que reconstruye en cada llamada — medido: 45% del pliegue con 40 fondos,
0,02 ms con los 2 que existen hoy.

**Y la más grande, que no metí en este commit:** la pantalla elige el español
buscando pedazos del texto en inglés del servidor
(`why.includes("before a whole month")`). Si alguien corrige esa frase, los
cuatro motivos de la AC-2 se degradan al genérico y nada falla. Son ~40 líneas
y 6 archivos. Va con una llave estable en `ChargeMark`, no con más substring.
