---
skill: verify
agent_id: reviewer-1
started: 2026-08-02T1245
ended: 2026-08-02T1402
checkpoint: 7
artifacts:
  - features/007-recurring-engine/handoffs/2026-08-02T1258-refine.md
  - features/007-recurring-engine/handoffs/2026-08-02T1303-verify.md
  - docs/adr/0037-a-recurring-item-that-ended-is-derived-at-read-time-resuming-closes-the-paused-stretch.md
exit_criteria:
  - criterion: Every acceptance scenario passes, re-run by the verifier
    verified_by: tool
    met: true
    evidence: "`./run-acceptance-tests.sh` -> '201 passed, 1 warning in 5.29s'. Feature 007 collects 77 executions (70 Scenario blocks, one an Outline with 8 rows) — matches the CP3 IR count. 002/005/006 green as the regression lock. Re-run four times across three review rounds, including after each of my own edits"
  - criterion: The unit suites pass on the touched surface, re-run by the verifier
    verified_by: tool
    met: true
    evidence: "backend `QUAESTOR_DB=sqlite:// uv run pytest -q` -> '874 passed, 1 warning in 17.09s' (863 -> 869 -> 874 as the reworks added tests). frontend `pnpm vitest run` -> '42 passed (42) / 233 passed (233)'. `pnpm biome check` over the 13 changed frontend files -> clean"
  - criterion: Verification is independent of the implementer (Principle 7)
    verified_by: tool
    met: true
    evidence: "CP5 handoff `2026-08-02T1240-implement.md` carries `agent_id: main`; this handoff and the CP6 one carry `agent_id: reviewer-1`. `dae_handoff.py <feature> --through 5` -> 'ok -- latest complete checkpoint: 5'"
  - criterion: The implementation matches its recorded decisions (plan + ADRs)
    verified_by: judgment
    met: true
    evidence: "Read ADR-0037 line by line against the code: title, status, Option 6 with pros/cons, decision outcome switched, the false 'provably unaffected' claim removed from Consequences and quoted-then-disowned in the Amendment, index row retitled, product-decisions.md line 411 qualified accurately. I fixed one leftover Consequences bullet still asserting the Option-4 behaviour. plan.md § D3 has since been rewritten to the shipped design and no stale `close_paused_stretch` reference survives anywhere (`grep -rn` -> none). Schema surface matches both the plan and the migrations exactly: `git diff models.py` shows two enum values, zero columns, zero tables"
  - criterion: No defect was found that moves money incorrectly
    verified_by: judgment
    met: true
    evidence: "The D1 money defect is fixed by behaviour change and re-verified: the original repro now leaves the two downtime dates `offered` (inert) instead of `skipped`, and AC-17's balance is unchanged. D4 re-probed: a mid-set failure in `accept_pending_dates` leaves balance and tx count exactly as before, all remaining dates still offered, no duplicate or orphan occurrence rows, and a retry after fixing the account succeeds reusing the same rows. No path remains that charges or writes off money without the user's answer"
  - criterion: The new offer cap is enforced everywhere it should be and nowhere it should not
    verified_by: tool
    met: true
    evidence: "True only after two fixes I applied. Boundary probed: 59 and 60 accepted, 61 refused with zero recurring_item and zero occurrence rows left behind. Placement is right — enforced on declare and on moving the start back, deliberately absent from `offer_paused_stretch` (a 400-date resume offers 400, by design). Two defects found and fixed: B1, the guard read `item.end_date` before the new one was assigned, so `PATCH` with start_date+end_date together slipped 2437 rows past a cap of 60 — reachable from REST and MCP, and the recurring page's edit form always sends `end_date`, so it was live on the primary UI path; B2, the guard counted all due dates rather than unclaimed ones, falsely refusing a one-month start shift on a 5-year monthly obligation whose 61 dates were already charged. Both re-probed after the fix: bypass blocked, false rejection gone (1 date offered)"
  - criterion: The diff is fit for the default branch
    verified_by: tool
    met: true
    evidence: "Merge-reviewer sweep over all 48 changed/new .py/.ts/.tsx files: no TODO/FIXME/XXX/HACK, no debugger/breakpoint/pdb, no console.log; the only two `print(` are pre-existing CLI output in daily.py (in HEAD) and 0009's deliberate operator line the runbook tells you to record. No skipped or xfail tests. AST sweep: every `test_*` function contains an assertion or `raises`. AST sweep: zero unused imports across all 48 files. 840 insertions / 398 deletions, 2618 new lines"
  - criterion: No prohibited code comments in the changed code
    verified_by: tool
    met: true
    evidence: "4 introduced lines remain, all outside backend/src (now entirely clean): 2 bare lint directives (`# noqa: E402,F401` in acceptance/handlers/__init__.py matching 3 verbatim siblings; `# noqa: BLE001` in recurring_engine.py:183) and 3 inline prose comments in backend/tests/services/test_planned.py. The implementer's account says test_planned.py keeps only pre-existing comments; `git show HEAD:` proves 007 added those 3. Honestly false against CLAUDE.md. My judgement: not a merge blocker | AMENDED 2026-08-02 (second independent re-verification): all 5 prohibited lines removed in the working tree (the 3 prose ones were in test_budgets.py, not test_planned.py). Re-derived the tokenize scan over the worktree vs base 7978f01 — the only introduced comments left are JSDoc on the exported `hasEnded` and `useRetained`, allowed under the docstring carve-out. Behaviour preserved: AST shows the identical import set, runtime `_REGISTRY` holds 193 handlers with all 4 modules imported, acceptance 201 passed and backend 874 passed. Ruff confirmed non-existent (`uv run ruff check` -> 'Failed to spawn: ruff'), so the removed directives suppressed nothing. Caveat: 3 of the 4 removed `# noqa` lines were pre-007 — scope creep beyond this feature, flagged in the amendment body"
  - criterion: The acceptance specs were not weakened
    verified_by: tool
    met: true
    evidence: "features/007 spec.md untouched through all three rounds — mtime 09:37, before CP5 ended; still 28 ACs / 70 Scenario blocks / 77 executions. features/006 spec.md unchanged since the one approved rewrite. Two behaviour changes (skipped -> offered on resume; per-charge -> single commit on accept) required no scenario edit, which I confirmed: AC-17 asserts only charge count and balance, never the state the paused dates land in"
  - criterion: Migrations are applied to production
    verified_by: human
    met: true
    evidence: "Left false at the implementer's request — they are about to run runbook Phase A. Correctly not done by any agent (charter §7, manifest `low` on migrations/**). All five runbook steps `completed: false`. This is the one hard blocker: the branch writes `Source.recurring`, a value production's enum does not have, so the first daily run after merge fails on every charge until `just backup` && `just migrate` runs | AMENDED 2026-08-02 (independent re-verification): the human ran Phase A. Live local Postgres (ADR-0030) queried read-only — `alembic_version` = `0009`; `occurrencestatus` = posted/planned/skipped/`offered`; `source` = manual/agent/import_/`recurring`; zero `recurring_item` rows left with `type='income' AND mode='manual'` (3 incomes, all `auto`). Backup `QuaestorBackups/quaestor-local-2026-08-02.dump` proven genuinely pre-migration — `pg_restore --data-only --table=alembic_version` on it yields `0007`. Runbook steps 1/2/3/5 closed; step 4 (resolve-orphaned-planned-incomes) stays open by the user's explicit decision, 2 rows pending"
findings_summary: "Merge-ready once the migration runs. Three review rounds, six defects found, all fixed — two by the implementer changing behaviour (D1, D4) and four by me. This round: D4 verified clean under adversarial probing; D2's cap was correct in policy but wrong in measurement twice, and one of those was live on the primary UI edit path. Merge-reviewer sweep of the whole diff is clean. Two criteria stay false: 4 comment lines (not a blocker) and the migration (the blocker)"
human_action_needed: yes
human_action_kind: approval
recommended_next: "Runbook Phase A (`just backup` -> `just migrate` -> verify enum values), then browser QA, then merge. Re-open this handoff afterwards to close the migration criterion. CP8 harden can carry the residuals: the dirty-session leak on a rejected update, the two arch-guard indirections, and an `architecture:` section in .engineer/manifest.yml"
tracker_update: "none — consolidation.md Tier 1 row 5's acceptance-tests column left at ❌ until the migration criterion closes"
status: complete
---

# verify — handoff summary

## What I did

Third and final review round, weighted as a merge gate rather than a checkpoint
audit. Re-ran all three suites, attacked the new offer cap and the new
all-or-nothing accept specifically, swept the whole diff for anything I would
not want on `main`, and re-checked the comment claim against `git show HEAD:`
rather than against the implementer's account of it.

## Artifacts produced

Both handoffs. Code fixes are recorded under CP6.

## Findings

### Suites

`874` backend / `201` acceptance / `233` frontend, biome clean. All three
reproduce the implementer's numbers.

### D4 — verified clean, no changes needed

All-or-nothing holds under adversarial probing. Accepted 2 of 5 offered dates,
then archived the account and accepted the rest: balance unchanged, tx count
unchanged, all 3 remaining dates still `offered`. The single commit interacts
correctly with row reuse — the occurrence count stays equal to the due-date
count with no duplicates or orphans, and a retry after fixing the account
accepts all 4 cleanly against the same rows. The scoping argument in the
docstring is right: per-charge commit exists for an unattended batch, and this
is one obligation with the user watching one button.

### D2 — right policy, wrong measurement, twice

The cap itself is well placed. 59 and 60 accepted, 61 refused, and a refused
declaration leaves **zero** `recurring_item` and **zero** occurrence rows — the
before-the-write ordering works. Leaving `offer_paused_stretch` uncapped is a
defensible call for the reason given.

But the guard was not measuring what its own error message says, in two ways:

**B1 — the cap was walkable.** The guard ran *before* `item.end_date = end_date`,
so it computed against the pre-update end date while `offer_passed_dates` used
the new one. Creating an item with `start=2020-01-01, end=2020-01-15`, then
`PATCH`ing `start_date=2019-12-01, end_date=null`, was **accepted** and wrote
**2437** offered rows against a cap of 60. Reachable from REST and from the MCP
`update_recurring` tool — and the recurring page's edit form always transmits
`end_date`, so this was live on the primary UI path, not a theoretical one.
Fixed by moving the guard below the `end_date` assignment.

**B2 — the cap falsely refused real obligations.** It counted every due date in
the window, including ones already charged. A 5-year monthly obligation with all
61 dates charged could not have its start moved back by one month: refused at
"62 passed dates", when exactly **1** would have been offered. This would have
hit during browser QA on freshly migrated production data, where long-lived
obligations are the norm. Fixed by subtracting `existing_due_dates`.

**Residual, not fixed, your call:** the guard now counts only *newly* offered
dates, so an item with 55 already-pending offers can take 10 more and reach 65
in the dialog. Marginal, and the error message's wording ("would put N passed
dates up for decision") matches the new behaviour. Counting pending + new would
be stricter if you want it.

### Merge-reviewer sweep — clean

Across all 48 changed/new source files: no TODO/FIXME/XXX/HACK, no
debugger/breakpoint/pdb, no `console.log`. The only two `print(` calls are
pre-existing CLI output in `daily.py` (confirmed in HEAD) and 0009's deliberate
operator line that the runbook tells you to record. No skipped or xfail tests.
An AST sweep confirms every `test_*` function asserts something, and that there
are zero unused imports anywhere in the diff. Schema surface is exactly what the
plan and migrations claim: two enum values, zero columns, zero tables.

### The comment claim — checked against git, not against the summary

Down from ~20 to 4, and `backend/src/quaestor/**` is now entirely clean. But
your account says `test_planned.py` "keeps only comments that predate this
feature"; `git show HEAD:backend/tests/services/test_planned.py` does not
contain those 3 lines. Feature 007 added them. The other 2 remaining are bare
lint directives. Small, but you asked me to tell you rather than let the human
find it — that is twice now that the comment accounting has been optimistic, so
I would not restate it from memory a third time.

### Still open, for CP8 rather than for merge

- **The dirty-session leak on a rejected `update_recurring`.** A refused edit
  leaves the item mutated in the session (`session.dirty == True`); I probed it,
  and an unrelated later `commit()` persists `name='RENAMED' amount=999900
  start=2020-01-01`. The API and MCP paths close the session per request so
  nothing corrupts today. The pattern is pre-existing — `update_recurring`
  already had five raise-after-mutation points — but the cap adds a sixth on the
  most likely user error. The fix is to wrap the body in `try/except:
  session.rollback(); raise`, which restores the invariant for all six. I left
  it: restructuring a core function is yours, not a reviewer's, at this gate.
- The two arch-guard indirections (`st = OccurrenceStatus.x; o.status = st`,
  `setattr`) and the missing `architecture:` section in `.engineer/manifest.yml`
  that makes `dae_arch.py` a no-op.

## Human action needed?

Yes — approval to merge, after one mandatory step.

## Recommended next step

**Merge: yes — after `just backup` && `just migrate`.**

That is the only hard blocker. The branch writes `Source.recurring`, a value
production's enum does not have; unmigrated, the first daily run fails on every
charge. Everything else on my list is either fixed or explicitly deferred.

Two things to watch during browser QA, both known and neither a defect:

1. Resuming a long-paused obligation offers every date it left behind,
   uncapped — a 400-day pause produces a 400-checkbox dialog. Correct data,
   ugly screen.
2. Record the number `0009` prints (`repeating incomes switched to automatic:
   N`). If N > 0, runbook step 5 has real work: those obligations' already-
   materialized planned incomes stay planned and no screen resolves them.

What convinced me: across three rounds the implementer twice chose to change
behaviour rather than reword the record to match a bug — on D1, where amending
the ADR would have been far easier, and on D4. That is the disposition that
makes the rest of the diff trustworthy. The defects I found this round were
measurement errors in a new guard, not design errors, and every number reported
reproduced exactly.

## Amendment — 2026-08-02, independent re-verification

Both criteria that were left `met: false` were re-checked from scratch by a
reviewer who wrote none of this code and took none of the preceding record on
trust. One flips to true; one does not.

---

**Criterion:** No prohibited code comments in the changed code

**Original:** met: false — "4 introduced lines remain: `# noqa: E402,F401` in `acceptance/handlers/__init__.py`, `# noqa: BLE001` in `acceptance/handlers/recurring_engine.py:183`, and 3 inline prose comments in `backend/tests/services/test_planned.py`."

**Now:** met: **false** — still false, and the original itemisation was wrong on
the file and on the count.

**Evidence:**

`git status` -> "nothing to commit, working tree clean"; `git log -1 --oneline HEAD`
-> `91f31ae merge: feature 007 recurring-engine`. HEAD *is* the merge commit, so
no post-merge edit exists and the implementer's claim that the lines were
"removed since" cannot be true. Diff base is the merge's first parent, `7978f01`.

Every comment token in every changed `.py`/`.ts`/`.tsx` file was enumerated with
Python `tokenize` (and a hand-written string-aware scanner for TS/TSX) over the
post-merge blob, intersected with the added-line numbers from `git diff -U0`,
and each hit classified against `git show 7978f01:<path>`. **Five** prohibited
lines were introduced and all five are still present:

```
acceptance/handlers/__init__.py:89:      from . import recurring_engine  # noqa: E402,F401
acceptance/handlers/recurring_engine.py:183:     except Exception as exc:  # noqa: BLE001
backend/tests/services/test_budgets.py:169:     occurrences.materialize_due(session, date(2026, 6, 30))  # posts the recurring tx
backend/tests/services/test_budgets.py:183:     occurrences.materialize_due(session, date(2026, 6, 5))  # nothing due yet
backend/tests/services/test_budgets.py:185:     occurrences.materialize_due(session, date(2026, 6, 25))  # now a planned occurrence exists
```

Corrections: the three prose comments are in **`test_budgets.py`**, not
`test_planned.py` —
`git diff -U0 7978f01..91f31ae -- backend/tests/services/test_planned.py | grep '^+' | grep '#'`
returns nothing (exit 1) even though the file was modified. And the count is 5,
not 4; the original's own list summed to 5.

Also introduced but **correctly allowed**: JSDoc on the exported `hasEnded`
(`frontend/lib/date.ts:19-26`) and on the exported `useRetained`
(`frontend/lib/use-retained.ts:3-8`) — docstrings on public functions, which
CLAUDE.md explicitly permits. Correctly **not** attributable to 007:
`frontend/components/confirm-dialog.tsx:33`'s `// eslint-disable-next-line`,
which is untouched context present in `7978f01`.

Judgement, unchanged from the original in substance: not a merge blocker. Two of
the five are `# noqa` lint machinery, and `__init__.py:89` cannot simply be
deleted without breaking the E402 suppression its three verbatim siblings on
lines 87/88/90 rely on. The three `test_budgets.py` lines are the real
violation and are trivially removable. What is outstanding is a decision:
delete those three, and either amend CLAUDE.md to exempt lint directives or
move the suppressions into lint config.

**Verified by:** independent reviewer, not the implementer

---

**Criterion:** Migrations are applied to production

**Original:** met: false — "the one hard blocker: the branch writes `Source.recurring`, a value production's enum does not have, so the first daily run after merge fails on every charge until `just backup` && `just migrate` runs. All five runbook steps `completed: false`."

**Now:** met: **true**

**Evidence:** all queries read-only (`SELECT` / `pg_restore -f -`) against the
local Postgres container, which is production per ADR-0030. Render was not
contacted. No migration or `just` recipe was run by this reviewer.

Schema revision is at 0009:

```
$ ... psql -c "select version_num from alembic_version;"
 version_num
-------------
 0009
(1 row)
```

0008's two enum values are live. Read from the migration source rather than
guessed: `_NEW_VALUES = (("occurrencestatus", "offered"), ("source", "recurring"))`.

```
$ ... psql -c "select t.typname, e.enumlabel from pg_type t join pg_enum e on e.enumtypid = t.oid
                where t.typname in ('source','occurrencestatus') order by t.typname, e.enumsortorder;"
     typname      | enumlabel
------------------+-----------
 occurrencestatus | posted
 occurrencestatus | planned
 occurrencestatus | skipped
 occurrencestatus | offered
 source           | manual
 source           | agent
 source           | import_
 source           | recurring
(8 rows)
```

0009's effect landed. Its statement is `UPDATE recurring_item SET mode = 'auto'
WHERE type = 'income' AND mode = 'manual'`; no income is `manual` any more:

```
$ ... psql -c "select id, name, type, mode from recurring_item where type='income' order by id;"
 id |      name       |  type  | mode
----+-----------------+--------+------
  8 | Ubidots Salary  | income | auto
  9 | Ubidots Bonus   | income | auto
 10 | Keystone Salary | income | auto
(3 rows)

$ ... psql -c "select type, mode, count(*) from recurring_item group by type, mode order by type, mode;"
  type   |  mode  | count
---------+--------+-------
 expense | manual |    11
 income  | auto   |     3
(2 rows)
```

A pre-migration backup exists, and it is a real restore point rather than a file
that merely satisfies the date gate. `just backup` writes to
`$HOME/Library/Mobile Documents/com~apple~CloudDocs/QuaestorBackups`:

```
53k  2 Aug 14:07  quaestor-local-2026-08-02.dump
```

Its contents prove it was taken *before* the migration — restored read-only to
stdout, never into a database:

```
$ pg_restore --data-only --table=alembic_version -f - quaestor-local-2026-08-02.dump
COPY public.alembic_version (version_num) FROM stdin;
0007
\.

$ pg_restore --schema-only -f - ... | grep -A5 'CREATE TYPE public.occurrencestatus'
CREATE TYPE public.occurrencestatus AS ENUM ('posted', 'planned', 'skipped');
CREATE TYPE public.source AS ENUM ('manual', 'agent', 'import_');
```

The dump is at revision 0007 with the pre-007 enum shape; the live DB is at
0009 with both new values. The blocker named in the original evidence — the
first daily run failing on `Source.recurring` — is closed.

`runbook.md` updated accordingly: `backup-before-anything`,
`count-manual-recurring-incomes`, `apply-migrations-0008-0009` and
`verify-enum-values-live` are now `completed: true` with the live output as
evidence. `resolve-orphaned-planned-incomes` remains `completed: false` — two
income transactions are still `planned` (tx 1634 Ubidots 2026-07-30, tx 1635
Keystone 2026-07-31) and the user has decided to leave them pending for now.
That step has `blocking_acs: []` and does not gate the merge.

The recovered pre-migration inventory also answers W8, which step 2 asked for
and which nobody recorded at the time: the backup's `recurring_item` data shows
exactly three manual incomes (ids 8, 9, 10), so 0009 printed
`repeating incomes switched to automatic: 3`.

**Verified by:** independent reviewer, not the implementer

## Amendment 2 — 2026-08-02, second independent re-verification

**Criterion:** No prohibited code comments in the changed code

**Original:** met: false — 4 (in fact 5) introduced lines remained; the
implementer's first claim that they had been removed was disproved by
`git status` and the commit graph.

**Now:** met: **true** — this time the removal is real and re-derived, not taken
on the implementer's account.

**Evidence:**

`git diff 91f31ae -- <paths>` shows exactly three code files changed since the
merge, matching what was claimed:

```
acceptance/handlers/__init__.py
acceptance/handlers/recurring_engine.py
backend/tests/services/test_budgets.py
```

Re-ran the `tokenize`-based scan from scratch, against the **working tree** this
time rather than the merge commit, over every changed `.py`/`.ts`/`.tsx` file vs
base `7978f01`. Introduced comments remaining:

```
frontend/lib/date.ts:19-26         JSDoc on exported hasEnded
frontend/lib/use-retained.ts:3-8   JSDoc on exported useRetained
```

Both allowed. **Zero** prohibited introduced comments. All five previously
listed lines are gone; `acceptance/handlers/recurring_engine.py` now has no
comment tokens whatsoever.

The three comments still present in `test_budgets.py` (lines 155/156/171) were
each confirmed pre-007 by grepping their literal text against
`git show 7978f01:backend/tests/services/test_budgets.py` — all three PRESENT in
base, so leaving them is correct. Small correction: the implementer also listed
line 187, which no longer carries a comment at all.

Behaviour preservation for the `__init__.py` restructure, verified three ways
rather than reasoned about:

1. AST: old and new both import
   `['fx_read_time', 'planned_payments', 'recurring_engine', 'transactions_crud']`.
2. Runtime in the venv: `_REGISTRY` = **193** handlers, and each of the four
   modules reports `imported: True, attr-on-pkg: True`. A function-scoped
   `from . import x` still executes the module body, so `@step` registration is
   unaffected, and the import system still sets the submodule attribute on the
   package.
3. Suites, re-run by me: `./run-acceptance-tests.sh` -> `201 passed, 1 warning
   in 5.63s` (007 collecting its 77 executions); backend
   `874 passed, 1 warning in 19.00s` using the `set -a && . ./.env.local.sqlite`
   + `QUAESTOR_DB=sqlite://` invocation. Both reproduce exactly.

Ruff independently confirmed absent: no `[tool.ruff]`, no ruff in
`[dependency-groups]`, no `ruff.toml`, `grep -c 'name = "ruff"' backend/uv.lock`
-> `0`, no `.github/`, no `.pre-commit-config.yaml`, no lint recipe. Decisive:
`uv run ruff check --version` -> `error: Failed to spawn: ruff`. The removed
`E402/F401/BLE001` suppressions were suppressing a linter that cannot run.

**Scope creep, flagged plainly as asked:** three of the four removed
`# noqa: E402,F401` lines predate 007. Removing them is outside this feature's
changed code, and the diff against `main` now touches lines 007 never wrote. My
judgement is to keep it — the directive is per-line, so 007's own line could not
be cleaned without restructuring the four-line block, making the restructure the
minimal coherent fix rather than an opportunistic one, and it is verified
behaviour-identical. But it is genuinely a boundary crossed, and a merge reviewer
should be told rather than left to find it.

Two residuals, neither blocking: `acceptance/handlers/__init__.py:79` still
carries a pre-007 `# noqa: BLE001 — surfaced with traceability` suppressing the
same absent linter, so the file is now inconsistent with its own new standard;
and the replacement identifier
`_import_feature_modules_so_their_step_registrations_run` reproduces the removed
comment's prose as a name, which satisfies CLAUDE.md's "refactor to better names"
remedy but is a taste call worth the user's attention.

Follow-up already captured by the implementer in `.engineer/roadmap.md` as
`id:ruff-strict-lint` — nothing lints Python in this project at all, which is
the real gap the directives were masking.

**Verified by:** independent reviewer, not the implementer

---

**Tracker:** `.engineer/consolidation.md` Tier 1 row 5's `acc. tests` column
flipped `❌` -> `✅`. The CP7 `tracker_update` note set exactly one condition —
"left at ❌ until the migration criterion closes" — and that criterion closed in
Amendment 1. The file's own header defines the column independently: "generated
acceptance tests passing against the code", which I re-ran at `201 passed`
including 007's 77 executions. Both the note's condition and the column's
definition are satisfied, so the flip is warranted on either reading.
