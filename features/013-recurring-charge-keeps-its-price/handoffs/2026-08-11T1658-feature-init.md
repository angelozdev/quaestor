---
skill: feature-init
agent_id: main-session
feature: 013-recurring-charge-keeps-its-price
started: 2026-08-11T1650
ended: 2026-08-11T1658
checkpoint: 1.5
artifacts:
  - features/013-recurring-charge-keeps-its-price/feature.md
  - features/013-recurring-charge-keeps-its-price/handoffs/2026-08-11T1650-discuss.md
  - .engineer/roadmap.md
findings_summary: "Feature folder created from the discuss payload. Number 013, not 011: 011-monthly-report lives on the unmerged `monthly-report` branch, so scanning main alone would have collided — the same trap 012 documented. Branch `recurring-charge-keeps-its-price` cut from main at e2ba3ee. Roadmap item marked in-progress. Tracker is `local` — the feature files are the tracker, so the upsert is a no-op by design. No decomposition: removing the guard, offering the conversion at confirm time and migrating the two wrong rules are one idea — a rule holds the merchant's price — and shipping the guard removal without the confirm path would leave a rule whose charges nobody can complete. The migration keeps autonomy at medium for the feature while the manifest's path override independently caps migrations/** at low."
human_action_needed: yes
recommended_next: "/engineer.prime-context then /engineer.discover-acs. The owner owns the Ready contract — feature.md is his to review before CP2 opens."
tracker_update: "local — no-op upsert. Roadmap: recurring-currency-independent-of-account -> in-progress, feature_slug recurring-charge-keeps-its-price."
exit_criteria:
  - criterion: "feature.md exists and carries the Ready contract (title, slug, outcome, scope, status, autonomy_level)"
    verified_by: file
    met: true
    evidence: "features/013-recurring-charge-keeps-its-price/feature.md — status: ready, autonomy_level: medium, outcome + scope written from the four discuss decisions"
  - criterion: "slug is kebab-case, lowercase, ASCII, <=50 chars, leading letter"
    verified_by: inspection
    met: true
    evidence: "`recurring-charge-keeps-its-price` — 31 chars, kebab-case, leading letter"
  - criterion: "autonomy_level is within manifest.autonomy.allowed_levels and no path override caps it lower"
    verified_by: tool
    met: true
    evidence: "dae_resolve.py: allowed_levels ['low','medium'], default 'medium'. The override on backend/src/quaestor/migrations/** caps that path at low; the feature carries a migration, so that file is low while the feature stays medium. CHARTER §7 additionally requires the owner present for a migration touching real data."
  - criterion: "relevant_adrs all exist"
    verified_by: tool
    met: true
    evidence: "docs/adr/README.md rows present for 0031, 0035, 0036, 0038, 0045, 0051, 0052 — all status accepted. 0052 is the one this feature will supersede in CP4."
  - criterion: "feature number does not collide"
    verified_by: tool
    met: true
    evidence: "`git ls-tree -d --name-only HEAD features/` on main ends at 012; 011-monthly-report lives on branch monthly-report (commit e247204, unmerged). 013 allocated."
  - criterion: "branch exists and matches feature.md's branch field"
    verified_by: tool
    met: true
    evidence: "`git checkout -b recurring-charge-keeps-its-price` from main at e2ba3ee; feature.md branch field matches"
  - criterion: "folder layout complete (handoffs/, .build/, .build gitignored)"
    verified_by: file
    met: true
    evidence: "handoffs/ holds the discuss handoff and this one; .build/ created; `.gitignore:57` already carries `.build/`"
  - criterion: "roadmap back-link written in both directions"
    verified_by: tool
    met: true
    evidence: "feature.md carries roadmap_ref: recurring-currency-independent-of-account; dae_roadmap.py mark returned status in-progress with feature_slug recurring-charge-keeps-its-price"
status: complete
---

# feature-init — resumen del handoff

**013-recurring-charge-keeps-its-price**, rama del mismo nombre, autonomía
media, `status: ready`.

## Por qué 013 y no 011

011-monthly-report existe solo en la rama `monthly-report` (`e247204`, sin
mergear). Contar carpetas sobre `main` habría dado 011 otra vez — el mismo
tropiezo que 012 dejó documentado. 011 queda intacta donde está.

## Por qué una sola feature y no tres

Quitar la atadura, ofrecer la conversión al confirmar y migrar las dos reglas
mal guardadas son una sola idea: **una regla guarda el precio del comercio.**
Entregar solo la primera dejaría reglas cuyos cobros nadie puede completar; solo
la tercera reescribiría dos filas que el código volvería a rechazar.

## La autonomía tiene dos niveles a propósito

La feature es `medium`. El archivo de migración queda en `low` por la regla de
rutas del manifiesto, sin importar el nivel de la feature — y CHARTER §7 exige
además tu presencia y un respaldo fresco antes de que toque datos reales.

## Lo que falta antes de CP2

La ficha es tu contrato. Te toca leerla antes de que abra el descubrimiento de
criterios.

```
DAE ▸ 013-recurring-charge-keeps-its-price
·0 Onboard · ▶1.5 Ready · ·2 ACs · ·3 Spec · ·4 Plan · ·5 Implement · ·6 Refine · ·7 Verify · ·8 Harden
```
