# Python
__pycache__/
*.py[cod]
.venv/
.python-version

# python / quaestor
__pycache__/
*.pyc
.venv/
backend/.venv/
.pytest_cache/
uv.lock

# Node / Next
node_modules/
.next/
out/

# OS
.DS_Store

# MCP — keep template .mcp.json committed, local overrides stay private
.mcp.local.json

# The `db` service runs Postgres 17; row data lives in the quaestor-db-data Docker volume.

# Deploy artifacts / state
.env
.env.*
!.env.example
ts-serve.local.json
litestream.state
backend/.docker_cache/
frontend/.next/
frontend/.docker_cache/

# Docker named volumes on the host (quaestor-db-data, quaestor-backups, etc.)
# when restored outside compose (rare; manual file copies).
quaestor_backups/

# AI agent workspace (oh-my-opencode orchestration state).
# https://github.com/code-yeongyu/oh-my-openagent — `.omo/` is tool state,
# regenerated each session. `.omo/rules/` is the project-shared rules
# directory and IS tracked.
.omo/*
!.omo/rules/
!.omo/rules/**

# One-off importer artifacts (work-in-progress; not source)
backend/migrate_lunchmoney.sql
