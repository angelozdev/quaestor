---
skill: post-merge
agent_id: main
started: 2026-08-03T1455
ended: 2026-08-03T1500
checkpoint: null
artifacts: []
exit_criteria:
  - criterion: Merge state confirmed before deleting anything
    verified_by: tool
    met: true
    evidence: "`git merge-base --is-ancestor mandatory-categories main` -> true, so every commit on the branch is reachable from main. NOTE the deviation: the skill's canonical probe is against `origin/HEAD`, and that one FAILS here — main is 25 commits ahead of origin/main because nothing has been pushed. `gh pr view mandatory-categories` -> 'no pull requests found', for the same reason. The branch is merged locally, not shipped"
  - criterion: Default branch synced with the remote
    verified_by: tool
    met: false
    evidence: "NOT MET, by the owner's sequencing rather than by failure. `git pull --ff-only` -> 'Already up to date', but no push was run: the owner authorised the merge and has not authorised publishing. main sits 25 commits ahead of origin/main, and origin carries none of feature 008 — not the code, not the two ADRs, not the migration that is already live in production"
  - criterion: Merged branches deleted non-destructively
    verified_by: tool
    met: true
    evidence: "`git branch -d mandatory-categories` (never -D) -> 'Deleted branch mandatory-categories (was 4203fab)'. -d refuses unmerged work by construction. The commits survive the ref: they are reachable from main, which contains the merge 60ec10e"
  - criterion: Stale remote refs pruned
    verified_by: tool
    met: true
    evidence: "`git fetch origin --prune` -> no refs removed. Remaining: origin/main and origin/fx-read-time-conversion. The branch just deleted never existed on the remote, so there was nothing to prune for it"
  - criterion: Tracker reflects the merge
    verified_by: tool
    met: true
    evidence: "`dae_roadmap.py mark mandatory-categories shipped mandatory-categories` -> status now `shipped` with feature_slug set, confirmed by `dae_roadmap.py read`. features/008-mandatory-categories/feature.md was already `done` (commit 4203fab)"
findings_summary: "Feature 008 merged into main as 60ec10e and the branch deleted. The merge is local only: main is 25 commits ahead of origin/main and origin has none of feature 008. Gates re-run on main after the merge rather than on the branch — backend 908, acceptance 246, frontend 243, just lint 0"
human_action_needed: yes
human_action_kind: decision
recommended_next: "`git push origin main` — 25 commits, and the only copy of feature 008 outside this disk. Then /engineer.next → 003-sinking-funds"
tracker_update: "roadmap id:mandatory-categories in-progress -> shipped; feature.md already done"
status: partial
---

# post-merge — handoff summary

## What I did

Merged feature 008 into `main` and cleaned the branch, at the owner's explicit
instruction ("listoo, merge a main") — charter §7 reserves merges to `main` for
a human, and this was one.

Gates were re-run **on `main` after the merge**, not taken from the branch:
backend 908, acceptance 246, frontend 243 across 43 files, `just lint` exit 0.

## What changed

- `git merge --no-ff mandatory-categories` → **60ec10e**, matching the
  convention feature 007 set. 21 commits from the branch, fast-forwardable but
  deliberately not fast-forwarded.
- `git branch -d mandatory-categories` → deleted, was `4203fab`.
- `git fetch origin --prune` → nothing stale to remove.
- Roadmap `mandatory-categories`: `in-progress` → `shipped`.

## Findings

**The skill's own merge probe fails here, and the reason matters.**
`git merge-base --is-ancestor HEAD origin/HEAD` is the canonical check, and it
returns false: `main` is 25 commits ahead of `origin/main`. I confirmed the
branch against **local `main`** instead. That is sound for deleting the ref —
`git branch -d` refuses unmerged work, and every commit is reachable from the
merge commit — but it is a weaker guarantee than the skill intends. The branch
is merged; the work is not shipped.

**Nothing about feature 008 exists off this machine.** Not the code, not
ADR-0041/0042, not the runbook recording how revision `0010` was applied. And
`0010` **is already live in production** — so the database and the only copy of
the code that respects it now depend on the same disk. That asymmetry is the
one thing worth closing soon.

**`origin/fx-read-time-conversion` is still on the remote**, unchanged from the
previous post-merge handoff. Deleting a remote branch is outward-facing and
stays a human call.

## Human action needed?

Yes — one decision, already framed: `git push origin main`.

## Recommended next step

Push, then `/engineer.next` → **003-sinking-funds**. It is `ready`, and 008 was
sequenced ahead of it precisely so its funding rules read clean categories.
